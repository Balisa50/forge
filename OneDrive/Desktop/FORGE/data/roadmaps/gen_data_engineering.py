#!/usr/bin/env python3
"""
Generate the raw Data Engineering curriculum: 24 weeks, 7 distinct day-topics each,
with a unique lesson body + labelled exercise per day, plus 3 concept-check MCQs/week.
Output: data-engineering-src.json  (raw, fed to enrich_track.py -> data-engineering.json)
"""
import json
from pathlib import Path

HERE = Path(__file__).resolve().parent


def lesson_body(title, concept, why, practice):
    return (
        f"## {title}\n\n{concept}\n\n"
        f"## Why it matters\n{why}\n\n"
        f"## In practice\n{practice}"
    )


def ex(label, instruction, checks):
    body = f"[{label}] {instruction}\n\nPASS:\n" + "\n".join(f"[x] {c}" for c in checks)
    return {"kind": "exercise", "title": "Your turn", "body": body}


# Each day: (title, concept, why, practice_markdown, exercise_label, exercise_instruction, [pass_checks])
# Weeks 1-10 are foundations; 11-24 build a progressive ingest->transform->warehouse->dashboard capstone.
WEEKS = [
    # ---------------- WEEK 1 ----------------
    dict(
        title="What data engineering actually is",
        phase="Foundations",
        context=("Data engineering is the discipline of building the systems that move, store, and shape data so that "
                 "analysts and ML models can trust it. Before you touch a single tool, you need the mental model: where "
                 "data comes from, where it goes, and who depends on it downstream. This week you map the landscape and "
                 "sketch your first pipeline on paper."),
        days=[
            ("Data engineering vs analytics vs software engineering",
             "Data engineers build the pipelines and storage that feed analysts and models; analysts query and interpret; software engineers build the apps that generate data in the first place.",
             "Knowing the boundary tells you what you own (reliable data delivery) versus what you consume or hand off.",
             "List the three roles and one deliverable each. A useful framing:\n\n```text\nSoftware eng  -> produces data (app events, orders)\nData eng      -> moves + shapes data (pipelines, warehouse)\nAnalyst/ML    -> consumes data (dashboards, models)\n```",
             "WRITE", "In your own words, write three sentences defining each role and what they hand to the next.",
             ["You named a concrete deliverable for each role", "You explained who consumes a data engineer's output"]),
            ("The data lifecycle: generation to consumption",
             "Data flows through generation, ingestion, storage, transformation, serving, and consumption. Each stage is a place something can break.",
             "If you can name the stage where a bug lives, you can fix it ten times faster.",
             "Trace one row of data end to end. The canonical lifecycle:\n\n```text\ngenerate -> ingest -> store -> transform -> serve -> consume\n```",
             "WRITE", "Pick a real example (a food order) and write what happens to it at each lifecycle stage.",
             ["You traced one record through all six stages", "You named a failure mode for at least two stages"]),
            ("Batch vs streaming: the core trade-off",
             "Batch processing handles data in scheduled chunks (hourly, daily); streaming handles each event as it arrives (sub-second to seconds).",
             "Choosing wrong means either stale dashboards or wildly over-engineered infrastructure.",
             "Decide batch or streaming for three scenarios. A rule of thumb:\n\n```text\nDaily sales report      -> batch (cheap, simple)\nFraud alert on payment  -> streaming (latency matters)\nMonthly payroll         -> batch\n```",
             "WRITE", "For three business scenarios, choose batch or streaming and justify the latency requirement in one line each.",
             ["You chose batch or streaming for three scenarios", "Each choice cites a latency need"]),
            ("The modern data stack",
             "A typical stack today is: ingestion (Fivetran/Airbyte) -> warehouse (BigQuery/Snowflake) -> transformation (dbt) -> BI (Looker/Power BI), wired together by an orchestrator.",
             "Almost every job description lists a subset of these; knowing the shape lets you read any architecture diagram.",
             "Label each layer of a stack diagram. The shape to memorise:\n\n```text\nsources -> ingestion -> warehouse/lake -> transform (dbt) -> BI\n                                  \\-> orchestration ties it together\n```",
             "WRITE", "Draw the modern data stack on paper and label each layer with one real tool.",
             ["You labelled all five layers", "You named a real tool per layer"]),
            ("Roles on a data team",
             "Beyond the data engineer you will work with analytics engineers, ML engineers, platform/infra engineers, and data product managers.",
             "Knowing who owns what keeps you from rebuilding something another team already maintains.",
             "Match responsibilities to roles. A quick map:\n\n```text\nAnalytics eng -> dbt models, metrics layer\nML eng        -> feature pipelines, serving\nPlatform eng  -> the warehouse + orchestration infra\n```",
             "WRITE", "Write who you would ask for: a broken dbt model, a slow warehouse, and a new feature table.",
             ["You assigned each problem to a role", "You justified one of the assignments"]),
            ("Idempotency and why reruns must be safe",
             "An idempotent pipeline produces the same result whether you run it once or five times; reruns are a daily reality, so non-idempotent jobs create duplicate or corrupted data.",
             "The single most common production data bug is a rerun that double-counts rows.",
             "Contrast a safe and an unsafe load. The pattern that saves you:\n\n```sql\n-- unsafe: appends duplicates on rerun\nINSERT INTO sales SELECT * FROM staging;\n-- safe: replace the partition\nDELETE FROM sales WHERE dt = '2026-06-06';\nINSERT INTO sales SELECT * FROM staging WHERE dt = '2026-06-06';\n```",
             "WRITE", "Explain in writing why the second pattern is safe to run twice and the first is not.",
             ["You defined idempotency in one sentence", "You explained the duplicate-row failure of the unsafe pattern"]),
            ("Sketch your first pipeline",
             "A pipeline sketch names the source, the destination, the transformation, the schedule, and the owner before any code is written.",
             "A five-line sketch catches design mistakes that would cost days to undo in code.",
             "Write a one-page pipeline spec. The template:\n\n```text\nSource:      public weather API\nDestination: BigQuery table weather.daily\nTransform:   parse JSON, keep city/date/temp\nSchedule:    daily at 06:00\nOwner:       you\n```",
             "PRODUCE", "Produce a one-page spec for a pipeline that loads a public API into a warehouse table, then commit it as SPEC.md.",
             ["SPEC.md names source, destination, transform, schedule, owner", "You committed it to your repo"]),
        ],
        cc=[
            ("Which responsibility most clearly belongs to a data engineer rather than an analyst?",
             ["Interpreting a dashboard for executives", "Building the pipeline that loads orders into the warehouse",
              "Designing the marketing campaign", "Writing the mobile app UI"], 1,
             "Data engineers own the movement and shaping of data into reliable storage. Interpreting dashboards is analytics work, and building the app UI is software engineering. The pipeline that loads orders is squarely the engineer's deliverable."),
            ("When is streaming the right choice over batch processing?",
             ["When a daily summary email is enough", "When fraud must be flagged within seconds of a payment",
              "When cost must be minimised above all", "When data only changes monthly"], 1,
             "Streaming earns its added complexity only when low latency is a real requirement, such as flagging fraud in seconds. Daily summaries and monthly data are classic batch cases. Streaming usually costs more, so it is not the cost-minimising default."),
            ("Why must a production pipeline be idempotent?",
             ["So it runs faster", "So a rerun does not duplicate or corrupt data", "So it uses less storage",
              "So it needs no orchestration"], 1,
             "Reruns happen constantly in production after failures and backfills. An idempotent job yields the same result no matter how many times it runs, which prevents the double-counting that is the most common data bug. Speed and storage are unrelated to this property."),
        ],
    ),
    # ---------------- WEEK 2 ----------------
    dict(
        title="OLTP vs OLAP, star and snowflake schemas",
        phase="Foundations",
        context=("Transactional databases (OLTP) and analytical databases (OLAP) are optimised for opposite goals. "
                 "This week you learn why you never run analytics directly on the app database, and you model your "
                 "first star schema: the backbone of every warehouse."),
        days=[
            ("OLTP: systems built for writes",
             "OLTP (online transaction processing) databases like Postgres and MySQL are tuned for many small, fast, concurrent writes that keep an application running.",
             "Running heavy analytics here will slow or crash the app your business depends on.",
             "Inspect a normalized OLTP shape. Typical rows are tiny and frequent:\n\n```sql\nINSERT INTO orders(user_id, total, created_at) VALUES (42, 19.99, now());\n```",
             "WRITE", "Write two reasons an OLTP database is a poor place to run a year-long aggregation query.",
             ["You named row-locking or contention with the app", "You contrasted write-tuning vs scan-tuning"]),
            ("OLAP: systems built for reads at scale",
             "OLAP (online analytical processing) databases like BigQuery and Snowflake are tuned for scanning billions of rows for aggregates, using columnar storage.",
             "Columnar layout reads only the columns a query needs, which is why analytics queries fly here.",
             "Compare a scan query's home. OLAP shines at:\n\n```sql\nSELECT country, SUM(total) FROM orders GROUP BY country;\n```",
             "WRITE", "Explain why columnar storage makes the query above cheaper than the same query on a row store.",
             ["You explained columns are read independently", "You linked this to analytics scan patterns"]),
            ("Normalization vs denormalization",
             "OLTP favours normalized tables (no duplicated data, many joins); OLAP often denormalizes to reduce joins and speed up scans.",
             "The right level of normalization depends entirely on whether you optimise for writes or for reads.",
             "Contrast the two shapes. Normalized splits; denormalized flattens:\n\n```text\nNormalized:   orders + users + products (3 tables, joins)\nDenormalized: order_facts (1 wide table, no joins)\n```",
             "WRITE", "Describe one benefit and one cost of denormalizing an analytics table.",
             ["You named a read-speed benefit", "You named a storage or update cost"]),
            ("Facts and dimensions",
             "A fact table stores measurable events (a sale, a click) with foreign keys; dimension tables store the descriptive context (who, what, when, where).",
             "Separating facts from dimensions is what makes a warehouse queryable by business questions.",
             "Identify facts vs dimensions. A sale decomposes as:\n\n```text\nfact_sales: sale_id, date_key, product_key, customer_key, amount\ndim_product, dim_customer, dim_date  (the context)\n```",
             "WRITE", "For a cinema ticket sale, list the fact measures and three dimensions.",
             ["You listed at least one numeric measure", "You named three dimension tables"]),
            ("The star schema",
             "A star schema places one central fact table surrounded by denormalized dimension tables, joined by surrogate keys.",
             "It is the default analytical model because it balances query speed with understandability.",
             "Wire up a star. The join pattern:\n\n```sql\nSELECT d.month, SUM(f.amount)\nFROM fact_sales f\nJOIN dim_date d ON f.date_key = d.date_key\nGROUP BY d.month;\n```",
             "PRODUCE", "Produce a star-schema diagram for an e-commerce dataset with one fact and three dimensions, committed as STAR.md.",
             ["STAR.md shows one fact table at the centre", "Three dimensions join via keys"]),
            ("The snowflake schema",
             "A snowflake schema normalizes dimensions into sub-dimensions (e.g., dim_product -> dim_category), trading some query speed for less redundancy.",
             "Knowing when to snowflake versus stay star keeps your model from becoming either bloated or slow.",
             "See the extra hop snowflaking adds:\n\n```text\nstar:      fact -> dim_product(category_name)\nsnowflake: fact -> dim_product -> dim_category(category_name)\n```",
             "WRITE", "Explain one situation where snowflaking a dimension is worth the extra join.",
             ["You named a redundancy or maintenance benefit", "You acknowledged the query-speed cost"]),
            ("Surrogate keys and slowly changing dimensions",
             "Surrogate keys are warehouse-generated integer keys that decouple the model from source IDs; slowly changing dimensions (SCD) track how descriptive attributes change over time.",
             "Without SCD handling, a customer who moves cities silently rewrites history in your reports.",
             "See a Type 2 SCD keep history with row versions:\n\n```text\ncustomer_key | customer_id | city     | valid_from | valid_to\n1            | 42          | Banjul   | 2025-01-01 | 2026-05-31\n2            | 42          | Serrekunda| 2026-06-01 | NULL\n```",
             "PRODUCE", "Model a Type 2 SCD for a customer dimension and commit the table definition.",
             ["Your table has valid_from / valid_to columns", "A surrogate key distinguishes versions"]),
        ],
        cc=[
            ("Why should analytics queries not run directly on an OLTP database?",
             ["OLTP cannot store dates", "Heavy scans contend with the app's fast writes and can degrade it",
              "OLTP has no SQL", "Analysts dislike Postgres"], 1,
             "OLTP systems are tuned for many small concurrent writes that keep an app responsive. A long analytical scan competes for the same locks and resources and can slow or crash the application. That is why analytics belongs in a separate OLAP system."),
            ("What does a fact table primarily store?",
             ["Descriptive context like names and categories", "Measurable events with foreign keys to dimensions",
              "Only primary keys", "Application source code"], 1,
             "A fact table holds the measurable events of the business, such as a sale amount, along with foreign keys. The descriptive context lives in dimension tables. This separation is what lets you slice measures by any dimension."),
            ("What problem does a Type 2 slowly changing dimension solve?",
             ["It speeds up joins", "It preserves historical attribute values when they change over time",
              "It removes the need for keys", "It compresses storage"], 1,
             "A Type 2 SCD keeps a new row each time a descriptive attribute changes, with valid-from and valid-to dates. This preserves history so reports about the past stay correct even after a customer moves or renames. It is about historical accuracy, not speed or compression."),
        ],
    ),
    # ---------------- WEEK 3 ----------------
    dict(
        title="Data warehouses: BigQuery, Redshift, Snowflake",
        phase="Foundations",
        context=("A cloud data warehouse is the analytical engine at the centre of the stack. This week you learn how "
                 "columnar, distributed warehouses price and partition data, and you load your first dataset into one."),
        days=[
            ("What a cloud data warehouse is",
             "A cloud warehouse separates storage from compute, stores data columnar and compressed, and scales query power on demand.",
             "Separation of storage and compute is why you can query a terabyte without owning a server farm.",
             "Note the architecture shift:\n\n```text\nold: one box holds data + compute (scaling = bigger box)\nnew: storage (cheap) <-> compute (elastic clusters)\n```",
             "WRITE", "Explain in two sentences why separating storage from compute lowers cost for spiky workloads.",
             ["You linked elastic compute to spiky demand", "You noted storage stays cheap when idle"]),
            ("BigQuery: serverless analytics",
             "BigQuery is fully serverless: you write SQL, Google manages the compute, and you pay per byte scanned or per slot-hour.",
             "Because you pay per byte scanned, SELECT * on a wide table is a literal money leak.",
             "Reduce bytes scanned by selecting only needed columns:\n\n```sql\n-- cheap: scans 2 columns\nSELECT country, total FROM `proj.sales.orders`;\n```",
             "CODE", "Run a BigQuery query on a public dataset and record the bytes-scanned estimate the editor shows.",
             ["You queried a public dataset", "You recorded the bytes-processed figure"]),
            ("Snowflake: virtual warehouses",
             "Snowflake separates storage from named virtual warehouses (compute clusters) that you size and auto-suspend independently.",
             "Auto-suspend means an idle warehouse stops billing within seconds, a key cost lever.",
             "Size compute to the job, not the data:\n\n```sql\nCREATE WAREHOUSE etl_wh WITH WAREHOUSE_SIZE='SMALL' AUTO_SUSPEND=60;\n```",
             "WRITE", "Describe how auto-suspend and warehouse sizing each control Snowflake cost.",
             ["You explained auto-suspend stops idle billing", "You linked size to query speed/cost"]),
            ("Redshift: provisioned clusters",
             "Redshift is AWS's warehouse built on provisioned node clusters with distribution and sort keys you choose explicitly.",
             "Choosing the wrong distribution key forces network shuffles that dominate query time.",
             "Distribution keys decide where rows live:\n\n```sql\nCREATE TABLE sales(...) DISTKEY(customer_id) SORTKEY(order_date);\n```",
             "WRITE", "Explain what a distribution key does and why a bad choice causes data shuffling.",
             ["You defined distribution key", "You linked it to cross-node shuffles"]),
            ("Partitioning and clustering",
             "Partitioning splits a table by a column (often date) so queries scan only relevant partitions; clustering co-locates similar rows to prune further.",
             "A date filter on a partitioned table can cut scanned bytes by 99 percent.",
             "Partition by date, cluster by a common filter:\n\n```sql\nCREATE TABLE events PARTITION BY DATE(ts) CLUSTER BY user_id AS SELECT ...;\n```",
             "CODE", "Create a partitioned table and run a query with and without a date filter, comparing bytes scanned.",
             ["You created a partitioned table", "You compared scanned bytes for filtered vs unfiltered queries"]),
            ("Warehouse cost models",
             "Warehouses bill either per byte scanned (BigQuery on-demand) or per compute-time (Snowflake/Redshift), so the cheapest design depends on the pricing model.",
             "The same query can be cheap on one warehouse and expensive on another purely due to the billing model.",
             "Match optimisation to billing:\n\n```text\nper-byte  -> select fewer columns, partition, filter early\nper-time  -> right-size compute, auto-suspend, cache results\n```",
             "WRITE", "For both billing models, list two concrete actions that reduce your bill.",
             ["You gave two per-byte tactics", "You gave two per-compute-time tactics"]),
            ("Load your first dataset",
             "Loading data means staging files (often in object storage), defining the target schema, and running a COPY/LOAD that the warehouse parallelises.",
             "Getting one real table loaded turns the warehouse from theory into your working environment.",
             "Load a CSV into a managed table:\n\n```sql\nLOAD DATA INTO sales.orders FROM FILES (format='CSV', uris=['gs://bucket/orders*.csv']);\n```",
             "PRODUCE", "Load a public CSV into a warehouse table of your choice and commit the load script.",
             ["A table exists with the loaded rows", "The load script is committed to your repo"]),
        ],
        cc=[
            ("What is the main architectural advantage of a cloud data warehouse?",
             ["It runs on your laptop", "It separates cheap storage from elastic, on-demand compute",
              "It only stores CSV", "It has no SQL interface"], 1,
             "Cloud warehouses decouple storage from compute, so you store data cheaply and spin compute up only when querying. This lets spiky analytical workloads scale without permanent hardware. It is the defining property that makes terabyte queries affordable."),
            ("On a per-byte-scanned warehouse like BigQuery on-demand, which habit saves the most money?",
             ["Always using SELECT *", "Selecting only needed columns and filtering partitioned tables",
              "Adding more nodes", "Disabling SQL caching"], 1,
             "When billing is per byte scanned, every unread column and partition you avoid is money saved. Selecting only needed columns and filtering on a partition key can cut scanned bytes dramatically. SELECT * does the opposite and inflates the bill."),
            ("Why does partitioning a table by date reduce query cost?",
             ["It encrypts the data", "A date filter lets the engine skip partitions it does not need",
              "It sorts rows alphabetically", "It removes duplicate rows"], 1,
             "Partitioning physically segments a table by a column such as date. A query that filters on that date only reads the matching partitions and skips the rest, cutting scanned data sharply. It is one of the highest-leverage warehouse optimisations."),
        ],
    ),
    # ---------------- WEEK 4 ----------------
    dict(
        title="ELT vs ETL and dbt fundamentals",
        phase="Foundations",
        context=("The industry shifted from ETL (transform before loading) to ELT (load raw, transform inside the "
                 "warehouse). dbt is the tool that made ELT transformations testable, version-controlled software. "
                 "This week you build your first dbt project."),
        days=[
            ("ETL: the historical approach",
             "ETL extracts data, transforms it on a separate processing server, then loads the clean result into the warehouse.",
             "ETL made sense when warehouse compute was scarce and expensive, but it hides transformation logic in opaque tools.",
             "Picture the order of operations:\n\n```text\nETL: extract -> transform (external box) -> load\n```",
             "WRITE", "Explain why scarce, expensive warehouse compute historically favoured ETL.",
             ["You linked ETL to limited warehouse compute", "You noted transform happened outside the warehouse"]),
            ("ELT: the modern shift",
             "ELT loads raw data into the warehouse first, then transforms it with SQL using the warehouse's own elastic compute.",
             "Cheap elastic warehouse compute flipped the equation: keep raw data and transform in place, in version-controlled SQL.",
             "Note the reorder:\n\n```text\nELT: extract -> load (raw) -> transform (SQL in warehouse)\n```",
             "WRITE", "Describe two advantages of keeping raw data in the warehouse before transforming.",
             ["You noted reprocessing without re-extracting", "You noted transform logic stays in SQL/version control"]),
            ("dbt: transformation as software",
             "dbt lets you write transformations as SELECT statements in version-controlled files, then builds them into tables/views with dependency ordering.",
             "dbt brought software practices (testing, versioning, docs, CI) to the previously wild west of SQL transforms.",
             "Initialise a project and run it:\n\n```bash\ndbt init analytics\ndbt run\n```",
             "CODE", "Initialise a dbt project against your warehouse and run the example models successfully.",
             ["dbt run completes without errors", "At least one model materialised in the warehouse"]),
            ("Sources and refs",
             "dbt sources declare raw input tables; ref() links models together so dbt can compute the correct build order automatically.",
             "Using ref() instead of hard-coded table names is what gives dbt its dependency graph and safe rebuilds.",
             "Reference an upstream model so dbt orders the build:\n\n```sql\n-- models/stg_orders.sql\nSELECT * FROM {{ source('shop','orders') }}\n-- models/fct_orders.sql\nSELECT * FROM {{ ref('stg_orders') }}\n```",
             "CODE", "Create a staging model and a downstream model linked with ref(), then run dbt and inspect the DAG.",
             ["fct model uses ref() to the stg model", "dbt builds them in the correct order"]),
            ("Materializations",
             "dbt models can be materialized as views, tables, incremental models, or ephemeral CTEs, trading freshness, cost, and speed.",
             "Choosing the right materialization is the difference between a 3-second and a 30-minute build.",
             "Set a materialization in config:\n\n```sql\n{{ config(materialized='incremental') }}\nSELECT * FROM {{ ref('stg_events') }}\n{% if is_incremental() %} WHERE ts > (SELECT max(ts) FROM {{ this }}) {% endif %}\n```",
             "WRITE", "Explain when you would choose an incremental model over a full table rebuild.",
             ["You linked incremental to large, append-mostly data", "You noted the cost/speed benefit"]),
            ("dbt tests",
             "dbt ships generic tests (unique, not_null, accepted_values, relationships) you declare in YAML, plus custom SQL tests.",
             "Tests turn silent data corruption into a failed build you notice before stakeholders do.",
             "Declare tests on a column in schema.yml:\n\n```yaml\nmodels:\n  - name: stg_orders\n    columns:\n      - name: order_id\n        tests: [unique, not_null]\n```",
             "CODE", "Add unique and not_null tests to a model and run dbt test, then deliberately break one to see it fail.",
             ["dbt test passes on clean data", "A deliberately broken test fails as expected"]),
            ("dbt docs and lineage",
             "dbt generates a documentation site with descriptions and an interactive lineage graph derived from your ref() calls.",
             "Auto-generated lineage answers the most-asked data question: where does this number come from?",
             "Generate and serve docs:\n\n```bash\ndbt docs generate\ndbt docs serve\n```",
             "PRODUCE", "Generate dbt docs for your project and commit a screenshot of the lineage graph.",
             ["dbt docs generate succeeds", "You committed the lineage graph image"]),
        ],
        cc=[
            ("What fundamentally enabled the shift from ETL to ELT?",
             ["Faster internet", "Cheap, elastic warehouse compute that makes in-warehouse transforms practical",
              "The decline of SQL", "Smaller datasets"], 1,
             "ELT loads raw data first and transforms inside the warehouse. This only became the default once warehouse compute turned cheap and elastic, removing the reason to transform on a separate box. The change is about economics of compute, not network speed."),
            ("Why use ref() instead of a hard-coded table name in dbt?",
             ["It looks cleaner only", "It builds dbt's dependency graph so models build in the right order",
              "It encrypts the query", "It is required by SQL"], 1,
             "ref() tells dbt that one model depends on another, which lets dbt compute a build order and rebuild safely. Hard-coded names break that graph and can cause models to build before their inputs exist. The dependency graph is dbt's core value."),
            ("What is the main benefit of dbt tests?",
             ["They speed up queries", "They catch data-quality problems as failed builds before stakeholders see them",
              "They reduce storage", "They replace the warehouse"], 1,
             "dbt tests assert expectations like uniqueness and non-null on your models. When data violates them the build fails, so you catch corruption early rather than after a wrong dashboard ships. They are about trust and early detection, not performance."),
        ],
    ),
    # ---------------- WEEK 5 ----------------
    dict(
        title="Data lakes: S3, Delta Lake, Iceberg",
        phase="Foundations",
        context=("A data lake stores raw and semi-structured data cheaply on object storage. Open table formats like "
                 "Delta Lake and Iceberg add warehouse-like reliability on top. This week you learn file formats, "
                 "partitioning, and the lakehouse pattern."),
        days=[
            ("Object storage as the lake foundation",
             "Object storage (S3, GCS, Azure Blob) stores files cheaply and durably at any scale, and is the substrate every data lake sits on.",
             "It is an order of magnitude cheaper than warehouse storage, which is why raw data lands here first.",
             "Put and list an object:\n\n```bash\naws s3 cp orders.parquet s3://my-lake/raw/orders/\naws s3 ls s3://my-lake/raw/orders/\n```",
             "CODE", "Upload a file to an object-storage bucket and list it back from the CLI.",
             ["The object appears in the bucket listing", "You used the CLI, not the web console"]),
            ("Why Parquet beats CSV",
             "Parquet is a columnar, compressed, typed file format; CSV is row-based, untyped, and bloated.",
             "Switching a pipeline from CSV to Parquet routinely cuts storage and scan cost by 5-10x.",
             "Convert and compare sizes:\n\n```python\nimport pandas as pd\ndf = pd.read_csv('orders.csv')\ndf.to_parquet('orders.parquet')  # smaller + typed\n```",
             "CODE", "Convert a CSV to Parquet and compare the two file sizes on disk.",
             ["A Parquet file was produced", "You recorded both file sizes"]),
            ("Partitioning files in the lake",
             "Lake data is partitioned by writing files into directory paths like dt=2026-06-06/, so engines prune irrelevant folders.",
             "Hive-style partition paths let query engines skip terabytes without reading them.",
             "Lay out partitioned paths:\n\n```text\ns3://lake/orders/dt=2026-06-05/part-0.parquet\ns3://lake/orders/dt=2026-06-06/part-0.parquet\n```",
             "WRITE", "Explain how a query filtering dt='2026-06-06' avoids reading other partitions.",
             ["You described directory pruning", "You linked it to scan-cost savings"]),
            ("The problem with raw lakes",
             "Plain file lakes have no transactions, no schema enforcement, and no easy updates, leading to corrupt or half-written data (the data swamp).",
             "Without ACID guarantees, a failed write leaves readers seeing partial, inconsistent files.",
             "See the failure a naive lake allows:\n\n```text\nwriter crashes mid-write -> readers see half a file -> bad results\n```",
             "WRITE", "List three reliability problems a plain object-storage lake has that a warehouse does not.",
             ["You named missing transactions", "You named missing schema enforcement"]),
            ("Delta Lake: ACID on the lake",
             "Delta Lake adds a transaction log over Parquet files, giving ACID transactions, schema enforcement, and time travel.",
             "The transaction log is what turns a pile of files into something you can trust like a table.",
             "Write and time-travel a Delta table:\n\n```python\ndf.write.format('delta').save('/lake/orders')\nspark.read.format('delta').option('versionAsOf', 0).load('/lake/orders')\n```",
             "WRITE", "Explain what Delta's transaction log provides that raw Parquet files cannot.",
             ["You named ACID transactions", "You mentioned time travel or schema enforcement"]),
            ("Apache Iceberg and open formats",
             "Iceberg is an open table format with hidden partitioning, schema evolution, and engine-agnostic metadata, increasingly the neutral standard.",
             "Open formats stop a single vendor from owning your data, since many engines can read the same tables.",
             "Note the portability promise:\n\n```text\nIceberg table <- read by Spark, Trino, Flink, Snowflake, BigQuery\n```",
             "WRITE", "Describe why an open table format reduces vendor lock-in compared to a proprietary warehouse format.",
             ["You noted multiple engines read the same tables", "You linked this to avoiding lock-in"]),
            ("The lakehouse pattern",
             "A lakehouse uses an open table format on object storage to serve both cheap lake storage and warehouse-grade queries from one copy of data.",
             "One copy of data for both analytics and ML avoids the cost and drift of maintaining a separate lake and warehouse.",
             "See the converged architecture:\n\n```text\nobject storage + Iceberg/Delta + query engine = lakehouse\n```",
             "PRODUCE", "Write an architecture note proposing a lakehouse for a sample company and commit it as LAKEHOUSE.md.",
             ["LAKEHOUSE.md names the storage, format, and engine", "You justified one copy of data over two systems"]),
        ],
        cc=[
            ("Why does raw data typically land in object storage first?",
             ["It is the only place SQL works", "It is far cheaper and infinitely scalable for raw files",
              "It enforces schemas automatically", "It runs the dashboards"], 1,
             "Object storage is cheap, durable, and scales without limits, making it ideal for landing large volumes of raw data. Warehouse storage costs more, so you stage raw files in the lake first. Schema enforcement is exactly what plain object storage lacks."),
            ("What does Parquet provide that CSV does not?",
             ["A spreadsheet UI", "Columnar layout, compression, and explicit types",
              "Automatic dashboards", "Built-in transactions"], 1,
             "Parquet stores data by column with compression and typed schemas, so engines read only needed columns and files are far smaller. CSV is row-based, untyped, and bloated by comparison. Transactions come from table formats like Delta, not from Parquet itself."),
            ("What core capability do Delta Lake and Iceberg add over plain Parquet files?",
             ["They make files bigger", "ACID transactions and reliable table semantics over object storage",
              "They delete the warehouse", "They convert data to CSV"], 1,
             "Delta and Iceberg layer a transaction log and metadata over Parquet, providing ACID guarantees, schema enforcement, and features like time travel. This turns a loose pile of files into a trustworthy table. Plain Parquet has no such transactional guarantees."),
        ],
    ),
    # ---------------- WEEK 6 ----------------
    dict(
        title="Workflow orchestration: Airflow and Dagster",
        phase="Pipelines",
        context=("Orchestrators schedule, sequence, retry, and monitor the tasks that make up a pipeline. This week you "
                 "learn DAGs, scheduling, retries, and the asset-based model, then build a small orchestrated pipeline."),
        days=[
            ("Why orchestration exists",
             "An orchestrator runs tasks in dependency order, on a schedule, with retries and alerting, replacing fragile cron scripts.",
             "The moment you have more than two dependent jobs, cron and shell scripts become unmaintainable.",
             "Contrast cron with an orchestrator:\n\n```text\ncron: 3 independent jobs, no dependency awareness, silent failures\norchestrator: DAG with order, retries, visibility, alerts\n```",
             "WRITE", "List three things an orchestrator gives you that a bare cron job does not.",
             ["You named dependency ordering", "You named retries or alerting"]),
            ("Airflow DAGs",
             "In Airflow a DAG (directed acyclic graph) is Python code that defines tasks and their dependencies.",
             "Because the DAG is plain Python, your pipeline definition is testable, reviewable code.",
             "Define a two-task DAG with a dependency:\n\n```python\nextract = PythonOperator(task_id='extract', python_callable=do_extract)\nload    = PythonOperator(task_id='load', python_callable=do_load)\nextract >> load  # load runs after extract\n```",
             "CODE", "Write an Airflow DAG with two dependent tasks and confirm the order in the UI graph view.",
             ["The DAG appears in the Airflow UI", "The graph shows the correct task order"]),
            ("Operators and tasks",
             "Operators are templated task types (PythonOperator, BashOperator, SQL operators); a task is one instance of an operator in a DAG.",
             "Picking the right operator means you write glue, not boilerplate, for common actions.",
             "Use a Bash and a SQL operator together:\n\n```python\nrun_sql = BigQueryInsertJobOperator(task_id='agg', configuration={...})\nnotify  = BashOperator(task_id='notify', bash_command='echo done')\n```",
             "WRITE", "Name three operator types and one job each is suited to.",
             ["You listed three operator types", "You matched each to a task"]),
            ("Scheduling and backfills",
             "A schedule_interval plus a start_date defines when DAG runs fire; backfilling runs the DAG for past intervals.",
             "Understanding scheduling intervals prevents the classic bug of pipelines that run a day late or twice.",
             "Set a daily schedule and reason about runs:\n\n```python\nDAG(schedule_interval='@daily', start_date=datetime(2026,6,1), catchup=True)\n```",
             "WRITE", "Explain what catchup=True does and one situation where you would set it False.",
             ["You explained backfilling of missed intervals", "You gave a reason to disable catchup"]),
            ("Retries, SLAs, and alerting",
             "Tasks can declare retries, retry delays, and SLAs; breaches trigger alerts so failures surface fast.",
             "Sensible retries absorb transient failures while SLAs catch the silent slowdowns retries would hide.",
             "Add resilience to a task:\n\n```python\nPythonOperator(task_id='load', retries=3, retry_delay=timedelta(minutes=5), sla=timedelta(hours=1))\n```",
             "CODE", "Configure retries and an SLA on a task, then force a failure and observe the retry behaviour.",
             ["A task retries on failure", "You observed retry attempts in the logs"]),
            ("Dagster and software-defined assets",
             "Dagster models pipelines as software-defined assets (the data objects you want to exist) rather than just tasks, making lineage and testing first-class.",
             "Thinking in assets ('this table should exist and be fresh') maps better to data than thinking in tasks.",
             "Declare an asset whose value other assets depend on:\n\n```python\n@asset\ndef stg_orders(): return extract_orders()\n@asset\ndef fct_orders(stg_orders): return transform(stg_orders)\n```",
             "WRITE", "Contrast Dagster's asset model with Airflow's task model in two sentences.",
             ["You described assets as the data to produce", "You contrasted it with task-centric DAGs"]),
            ("Build a small orchestrated pipeline",
             "A minimal real pipeline extracts from a source, loads to storage, and transforms, all sequenced and retryable by the orchestrator.",
             "Wiring even three tasks end to end teaches more than any diagram about ordering and failure.",
             "Compose the three stages into one DAG:\n\n```text\nextract_api >> load_to_lake >> transform_to_warehouse\n```",
             "PRODUCE", "Build and run a three-task orchestrated pipeline (extract, load, transform) and commit the DAG/asset code.",
             ["The three tasks run in order", "The pipeline code is committed to your repo"]),
        ],
        cc=[
            ("What is the main reason to use an orchestrator instead of cron?",
             ["Cron cannot run Python", "It manages task dependencies, retries, and visibility across many jobs",
              "Cron is more expensive", "Orchestrators remove the need for code"], 1,
             "Orchestrators run tasks in dependency order with retries, scheduling, and monitoring built in. Cron treats each job independently and fails silently, which collapses once jobs depend on each other. The value is coordination and visibility, not language support."),
            ("In Airflow, what does the expression `extract >> load` mean?",
             ["Run them in parallel", "load runs only after extract succeeds",
              "Delete extract", "Merge the two tasks"], 1,
             "The >> operator sets a dependency so that load runs after extract completes successfully. It is how you express ordering in an Airflow DAG. The tasks are not merged or parallelised by this expression."),
            ("How does Dagster's asset model differ from Airflow's task model?",
             ["It has no scheduling", "It centres on the data objects to produce rather than on tasks to run",
              "It cannot retry", "It only works on CSV"], 1,
             "Dagster models software-defined assets, meaning the tables and datasets you want to exist, and derives execution from them. Airflow centres on tasks and their order. The asset framing makes lineage and freshness first-class concerns."),
        ],
    ),
    # ---------------- WEEK 7 ----------------
    dict(
        title="Streaming basics: Kafka and Kinesis",
        phase="Pipelines",
        context=("Streaming systems process events the moment they occur. This week you learn topics, partitions, "
                 "producers and consumers, delivery guarantees, and windowing, then publish and consume your first stream."),
        days=[
            ("Streams vs batch, revisited",
             "A stream is an unbounded, continuously arriving sequence of events; batch is a bounded chunk processed on a schedule.",
             "Modelling data as an unbounded stream changes how you think about completeness and time.",
             "See the mental shift:\n\n```text\nbatch:  [ today's file ] -> process once\nstream: ...e e e e e e e... -> process as each arrives\n```",
             "WRITE", "Give two example data sources that are naturally streams and two that are naturally batch.",
             ["You listed two streaming sources", "You listed two batch sources"]),
            ("Kafka topics and partitions",
             "Kafka organises events into topics, which are split into partitions that enable parallelism and ordering within a partition.",
             "Partitions are the unit of parallelism and the only place Kafka guarantees ordering.",
             "Create a topic with partitions:\n\n```bash\nkafka-topics --create --topic orders --partitions 3 --replication-factor 1\n```",
             "CODE", "Create a Kafka topic with multiple partitions and describe it from the CLI.",
             ["A topic with >1 partition exists", "You described the topic from the CLI"]),
            ("Producers and consumers",
             "Producers append events to a topic; consumers read them, tracking their position with an offset, often in consumer groups for scale.",
             "Offsets are how a consumer resumes exactly where it left off after a restart.",
             "Produce and consume from the CLI:\n\n```bash\nkafka-console-producer --topic orders\nkafka-console-consumer --topic orders --from-beginning\n```",
             "CODE", "Publish three messages to a topic and consume them from the beginning.",
             ["Three messages were produced", "A consumer read them back"]),
            ("Offsets and consumer groups",
             "A consumer group splits a topic's partitions among members so the group scales horizontally while each partition is read once.",
             "Consumer groups are how you scale stream processing without double-processing events.",
             "Reason about group assignment:\n\n```text\ntopic: 3 partitions, group of 3 consumers -> 1 partition each\nadd a 4th consumer -> it sits idle (no spare partition)\n```",
             "WRITE", "Explain why adding more consumers than partitions does not increase throughput.",
             ["You linked parallelism to partition count", "You noted extra consumers stay idle"]),
            ("Delivery guarantees",
             "Streaming systems offer at-most-once, at-least-once, or exactly-once delivery, trading performance against duplicate risk.",
             "Most real systems use at-least-once plus idempotent consumers to approximate exactly-once cheaply.",
             "Compare the three guarantees:\n\n```text\nat-most-once : may lose events, never duplicates\nat-least-once: never loses, may duplicate (dedupe downstream)\nexactly-once : no loss, no dup (costlier)\n```",
             "WRITE", "Explain how an idempotent consumer makes at-least-once delivery behave like exactly-once.",
             ["You defined at-least-once", "You linked idempotency to safe duplicates"]),
            ("Amazon Kinesis",
             "Kinesis is AWS's managed streaming service with shards (like partitions) and tight integration with Lambda and Firehose.",
             "Managed streaming removes the operational burden of running your own Kafka cluster.",
             "Note the Kafka-to-Kinesis vocabulary map:\n\n```text\nKafka topic     ~ Kinesis stream\nKafka partition ~ Kinesis shard\n```",
             "WRITE", "Map three Kafka concepts to their Kinesis equivalents and note one trade-off of managed streaming.",
             ["You mapped topic/partition/consumer", "You named a managed-vs-self-hosted trade-off"]),
            ("Windowing over streams",
             "Because streams are unbounded, aggregations use time windows (tumbling, sliding, session) to produce finite results.",
             "Windowing is how you answer 'how many orders in the last 5 minutes' over never-ending data.",
             "Picture a tumbling window:\n\n```text\n|--5min--||--5min--||--5min--|  each event lands in exactly one bucket\n```",
             "PRODUCE", "Write a short design for a 5-minute tumbling-window order count and commit it as STREAM.md.",
             ["STREAM.md specifies the window type and size", "You described how late events are handled"]),
        ],
        cc=[
            ("What is a Kafka partition the unit of?",
             ["Encryption", "Parallelism and per-partition ordering", "Billing", "Schema storage"], 1,
             "A topic is divided into partitions, which are consumed in parallel and preserve ordering within each partition. They are the fundamental scaling and ordering unit in Kafka. They are not about encryption or billing."),
            ("Why does adding more consumers than partitions not raise throughput?",
             ["Consumers crash", "Each partition is read by only one consumer in the group, so extras stay idle",
              "Kafka caps consumers at two", "Throughput depends only on disk"], 1,
             "Within a consumer group each partition is assigned to exactly one consumer. Once consumers equal partitions, additional consumers have nothing to read and sit idle. Throughput therefore scales with partition count, not raw consumer count."),
            ("How do teams cheaply approximate exactly-once processing?",
             ["By disabling retries", "By using at-least-once delivery with idempotent consumers",
              "By using at-most-once", "By writing to CSV"], 1,
             "True exactly-once is costly, so most systems choose at-least-once delivery and make consumers idempotent. Because reprocessing a duplicate has no harmful effect, the end result behaves as if exactly-once. At-most-once risks data loss and is rarely acceptable."),
        ],
    ),
    # ---------------- WEEK 8 ----------------
    dict(
        title="Batch processing with Spark",
        phase="Pipelines",
        context=("Apache Spark is the workhorse for processing data too big for one machine. This week you learn its "
                 "distributed architecture, DataFrame API, lazy evaluation, shuffles, and tuning, then run a real job."),
        days=[
            ("Why distributed processing",
             "When data exceeds one machine's memory or time budget, you split work across a cluster of executors coordinated by a driver.",
             "Spark exists for the moment pandas runs out of RAM on a single box.",
             "See the division of labour:\n\n```text\ndriver  -> plans the job, splits work\nexecutors -> run tasks on partitions in parallel\n```",
             "WRITE", "Explain the roles of the Spark driver and executors in two sentences.",
             ["You described the driver's planning role", "You described executors running tasks"]),
            ("RDDs vs DataFrames",
             "RDDs are Spark's low-level distributed collections; DataFrames add a typed, optimised, SQL-like API on top via the Catalyst optimiser.",
             "DataFrames let the optimiser rewrite your query, so they are almost always faster than hand-written RDD code.",
             "Prefer the DataFrame API:\n\n```python\ndf = spark.read.parquet('s3://lake/orders')\ndf.groupBy('country').sum('total').show()\n```",
             "CODE", "Load a Parquet file into a Spark DataFrame and run a groupBy aggregation.",
             ["A DataFrame loaded from a file", "A groupBy aggregation produced output"]),
            ("Transformations and actions",
             "Transformations (select, filter, groupBy) are lazy and build a plan; actions (count, collect, write) trigger execution.",
             "Knowing the lazy/eager split explains why nothing runs until you call an action.",
             "See lazy then eager:\n\n```python\nfiltered = df.filter(df.total > 100)  # lazy, nothing runs\nfiltered.count()                       # action, now it runs\n```",
             "WRITE", "Classify select, filter, groupBy, count, and write as transformation or action.",
             ["You correctly split the five operations", "You stated which kind triggers execution"]),
            ("Partitions and parallelism",
             "Spark splits data into partitions processed in parallel; too few underuses the cluster, too many adds overhead.",
             "Partition count is the main dial between an idle cluster and a thrashing one.",
             "Inspect and repartition:\n\n```python\ndf.rdd.getNumPartitions()\ndf = df.repartition(200)\n```",
             "WRITE", "Explain one symptom of too few partitions and one of too many.",
             ["You named underutilisation for too few", "You named overhead/small-files for too many"]),
            ("Shuffles and wide transformations",
             "Wide transformations (joins, groupBy) shuffle data across the network between partitions, the most expensive operation in Spark.",
             "Most Spark performance work is about reducing or optimising shuffles.",
             "Spot the shuffle-causing step:\n\n```python\n# join triggers a shuffle of both sides on the key\norders.join(customers, 'customer_id')\n```",
             "WRITE", "Explain why a join causes a shuffle and one way to reduce its cost.",
             ["You explained data moves across the network by key", "You named a mitigation (broadcast, fewer keys)"]),
            ("Broadcast joins and tuning",
             "Broadcasting a small table to every executor avoids shuffling the large table, a key tuning trick.",
             "A broadcast join can turn a 20-minute shuffle join into seconds when one side is small.",
             "Force a broadcast of the small side:\n\n```python\nfrom pyspark.sql.functions import broadcast\norders.join(broadcast(small_dim), 'product_id')\n```",
             "WRITE", "Explain when a broadcast join is appropriate and when it is dangerous.",
             ["You linked broadcast to a small table", "You noted the risk if the broadcast side is large"]),
            ("Run a real Spark job",
             "A complete Spark job reads input, transforms with the DataFrame API, and writes partitioned output back to storage.",
             "Shipping one end-to-end job cements reading, transforming, and writing at scale.",
             "Write partitioned output at the end of the job:\n\n```python\nresult.write.partitionBy('dt').parquet('s3://lake/curated/orders')\n```",
             "PRODUCE", "Write and run a Spark job that reads raw data, aggregates it, and writes partitioned Parquet; commit the code.",
             ["The job reads, transforms, and writes", "Output is partitioned Parquet and the code is committed"]),
        ],
        cc=[
            ("Why are Spark DataFrames usually preferred over RDDs?",
             ["They use less code only", "The Catalyst optimiser can rewrite DataFrame queries for speed",
              "RDDs cannot read files", "DataFrames avoid the cluster"], 1,
             "DataFrames expose a typed, declarative API that the Catalyst optimiser can analyse and rewrite into an efficient plan. RDDs are lower level and run exactly as written, missing those optimisations. So DataFrames are typically both shorter and faster."),
            ("In Spark, when does a transformation like filter actually run?",
             ["Immediately when defined", "Only when an action such as count or write is called",
              "Never", "On a fixed timer"], 1,
             "Transformations are lazy: they build a logical plan but do not execute. Execution is triggered only when an action like count, collect, or write is invoked. This laziness lets Spark optimise the whole chain before running it."),
            ("Why is a shuffle the operation you most want to minimise?",
             ["It deletes data", "It moves data across the network between partitions, which is expensive",
              "It is illegal", "It only runs on CSV"], 1,
             "Wide transformations such as joins and groupBy redistribute data across the cluster by key, sending it over the network. This network movement dominates runtime, so reducing shuffles is the heart of Spark tuning. Techniques like broadcast joins exist precisely to avoid it."),
        ],
    ),
    # ---------------- WEEK 9 ----------------
    dict(
        title="Data quality and testing",
        phase="Reliability",
        context=("Data that is wrong but confident is worse than no data. This week you learn to assert expectations "
                 "with dbt tests and Great Expectations, monitor freshness and anomalies, and gate pipelines in CI."),
        days=[
            ("Why data quality is non-negotiable",
             "Bad data silently propagates into dashboards and models, eroding trust that takes months to rebuild.",
             "A single wrong KPI in a board deck can undo a year of a data team's credibility.",
             "Categorise the quality dimensions:\n\n```text\ncompleteness, uniqueness, validity, freshness, consistency, accuracy\n```",
             "WRITE", "Define three data-quality dimensions and give a failing example of each.",
             ["You defined three dimensions", "Each has a concrete failure example"]),
            ("dbt tests as quality gates",
             "dbt's unique, not_null, accepted_values, and relationships tests assert table invariants on every build.",
             "Wiring tests into the build means corrupt data fails loudly instead of shipping silently.",
             "Assert a foreign-key relationship in YAML:\n\n```yaml\ncolumns:\n  - name: customer_id\n    tests:\n      - relationships: {to: ref('dim_customer'), field: customer_id}\n```",
             "CODE", "Add a relationships test between a fact and a dimension, then run dbt test.",
             ["A relationships test is declared", "dbt test executes it"]),
            ("Great Expectations",
             "Great Expectations defines expectation suites (declarative assertions) and validates batches of data against them with rich reports.",
             "It brings quality checks to data that does not live in dbt, like raw files and Spark outputs.",
             "Declare an expectation in Python:\n\n```python\nvalidator.expect_column_values_to_not_be_null('order_id')\nvalidator.expect_column_values_to_be_between('total', 0, 100000)\n```",
             "CODE", "Create an expectation suite with two expectations and validate a dataset against it.",
             ["A suite with >=2 expectations exists", "You ran a validation and saw the result"]),
            ("Schema tests and contracts",
             "Schema tests assert column names and types; data contracts formalise the agreement between producers and consumers so upstream changes do not silently break you.",
             "A contract turns a surprise schema change into a caught, negotiated event.",
             "Pin a schema expectation:\n\n```yaml\nmodels:\n  - name: stg_orders\n    config: {contract: {enforced: true}}\n    columns: [{name: order_id, data_type: int}]\n```",
             "WRITE", "Explain how a data contract protects you from an upstream column rename.",
             ["You described enforced schema expectations", "You linked it to upstream change safety"]),
            ("Freshness monitoring",
             "Freshness tests assert that data arrived recently enough; stale data is a common silent failure.",
             "A pipeline that succeeds but loads yesterday's data is worse than one that fails loudly.",
             "Declare a source-freshness rule in dbt:\n\n```yaml\nsources:\n  - name: shop\n    freshness: {warn_after: {count: 12, period: hour}}\n    loaded_at_field: _loaded_at\n```",
             "CODE", "Configure a source-freshness check and run dbt source freshness.",
             ["A freshness rule is configured", "You ran the freshness check"]),
            ("Anomaly detection",
             "Volume and distribution anomaly checks catch problems rules miss, like row counts suddenly halving.",
             "Many real incidents show up first as a metric that moved more than it ever has before.",
             "Sketch a simple volume check:\n\n```sql\n-- alert if today's rows deviate >50% from 7-day average\nSELECT abs(today - avg7) / avg7 > 0.5 AS anomaly FROM daily_counts;\n```",
             "WRITE", "Describe one volume anomaly and one distribution anomaly worth alerting on.",
             ["You gave a volume anomaly example", "You gave a distribution anomaly example"]),
            ("Quality gates in CI",
             "Running tests in CI on every pull request blocks bad transformations from ever reaching production.",
             "CI is where data quality shifts from hope to enforcement.",
             "Gate a merge on tests in CI:\n\n```yaml\n# .github/workflows/dbt.yml\n- run: dbt build   # runs models AND tests; fails the PR on any test failure\n```",
             "PRODUCE", "Add a CI workflow that runs your data tests on every pull request and commit it.",
             ["A CI workflow runs the tests", "The workflow fails when a test fails"]),
        ],
        cc=[
            ("Why is silently wrong data often worse than missing data?",
             ["It uses more storage", "It propagates into trusted dashboards and erodes credibility unnoticed",
              "It is always illegal", "It cannot be queried"], 1,
             "Missing data is obvious and prompts investigation, but wrong data that looks fine flows into dashboards and decisions unchallenged. By the time it is discovered, trust in the whole data platform is damaged. That hidden propagation is what makes it more dangerous."),
            ("What does a dbt relationships test assert?",
             ["That a column is encrypted", "That every value exists as a key in a referenced table",
              "That the table is small", "That queries are fast"], 1,
             "A relationships test checks referential integrity: each value in a column must exist in the referenced table's key column. It catches orphaned facts that point to missing dimensions. It is about correctness of joins, not performance or encryption."),
            ("Why run data tests in CI on every pull request?",
             ["To slow down merges", "To block bad transformations before they reach production",
              "To replace the warehouse", "To reduce storage"], 1,
             "Running tests in CI means a transformation that breaks an invariant fails the pull request and never merges. This shifts quality from a hope to an enforced gate. It is the same discipline software engineers apply to code, applied to data."),
        ],
    ),
    # ---------------- WEEK 10 ----------------
    dict(
        title="Data governance and cataloging",
        phase="Reliability",
        context=("As data grows, people cannot find it, trust it, or know what is sensitive. Governance and catalogs "
                 "solve discovery, lineage, classification, and access. This week you learn OpenMetadata, Amundsen, "
                 "lineage, and PII handling."),
        days=[
            ("What data governance covers",
             "Governance is the set of policies and tools for discovery, ownership, quality, lineage, classification, and access control over data.",
             "Without governance a growing data platform becomes an untrusted maze nobody can navigate.",
             "Map the pillars:\n\n```text\ndiscovery | ownership | lineage | classification | access | quality\n```",
             "WRITE", "List four governance pillars and one risk of neglecting each.",
             ["You listed four pillars", "Each has a stated risk"]),
            ("Metadata: the data about data",
             "Technical, business, and operational metadata describe schemas, meanings, and run history, powering search and trust.",
             "Rich metadata is what lets a new analyst find the right table in minutes, not days.",
             "Distinguish the metadata types:\n\n```text\ntechnical: column types, size\nbusiness:  'revenue = net of refunds'\noperational: last run, row count\n```",
             "WRITE", "Give one example each of technical, business, and operational metadata for a sales table.",
             ["You gave all three metadata examples", "Examples are specific to a sales table"]),
            ("Data catalogs: OpenMetadata and Amundsen",
             "A data catalog indexes tables, owners, descriptions, and usage so people can search and trust data, much like a search engine for your warehouse.",
             "A catalog turns tribal knowledge into searchable, durable documentation.",
             "Note what a catalog entry surfaces:\n\n```text\ntable -> owner, description, columns, freshness, popularity, lineage\n```",
             "WRITE", "Explain how a catalog reduces the time a new analyst needs to find a trusted table.",
             ["You described searchable metadata", "You linked it to faster onboarding"]),
            ("Data lineage",
             "Lineage traces how data flows from sources through transformations to outputs, at table and column level.",
             "Lineage answers both 'where did this number come from' and 'what breaks if I change this source'.",
             "Read a lineage chain:\n\n```text\nsource.orders -> stg_orders -> fct_orders -> dashboard.revenue\n```",
             "WRITE", "Describe two questions column-level lineage answers that table-level lineage cannot.",
             ["You named an impact-analysis question", "You named a root-cause question"]),
            ("PII and data classification",
             "Sensitive data (PII, financial, health) must be identified, tagged, and handled under stricter access and retention rules.",
             "Mishandling PII is not just a trust problem but a legal and regulatory one.",
             "Tag sensitive columns explicitly:\n\n```text\ndim_customer.email     -> PII\ndim_customer.card_last4 -> financial-sensitive\n```",
             "WRITE", "Identify three columns in a customer table that are PII and state how handling should differ.",
             ["You flagged three PII columns", "You described stricter handling"]),
            ("Access control and least privilege",
             "Role-based access control grants the minimum permissions each role needs, and masking hides sensitive fields from those who do not.",
             "Least privilege limits the blast radius of both mistakes and breaches.",
             "Grant scoped, not blanket, access:\n\n```sql\nGRANT SELECT ON analytics.fct_orders TO ROLE analyst;  -- not GRANT ALL\n```",
             "WRITE", "Explain least privilege and give one example of column masking for an analyst role.",
             ["You defined least privilege", "You gave a masking example"]),
            ("Catalog your capstone data",
             "Documenting your own datasets with owners, descriptions, and classifications is governance you can practise today.",
             "A catalog entry you write yourself is the fastest way to internalise what good metadata looks like.",
             "Fill a minimal catalog entry:\n\n```text\ntable: fct_orders | owner: you | desc: one row per order line\nclassification: contains customer_key (joins to PII)\n```",
             "PRODUCE", "Produce a CATALOG.md documenting two of your datasets with owner, description, and classification.",
             ["CATALOG.md documents two datasets", "Each entry has owner, description, and classification"]),
        ],
        cc=[
            ("What is the primary purpose of a data catalog?",
             ["To store the data itself", "To make data discoverable and trustworthy through searchable metadata",
              "To run transformations", "To replace the orchestrator"], 1,
             "A catalog indexes tables, owners, descriptions, lineage, and usage so people can find and trust data. It is a search engine and documentation layer, not a storage or compute system. Its payoff is faster discovery and higher trust."),
            ("What unique question does column-level lineage answer?",
             ["How big the table is", "Exactly which downstream columns a single source column feeds",
              "What the query cost is", "Who owns the warehouse"], 1,
             "Column-level lineage traces individual fields through transformations, so you can see precisely which outputs a given source column affects. Table-level lineage only shows table-to-table flow and misses that detail. This precision is vital for impact analysis and debugging."),
            ("What does the principle of least privilege achieve?",
             ["Faster queries", "It limits the damage from mistakes and breaches by granting only needed access",
              "Cheaper storage", "Automatic documentation"], 1,
             "Least privilege gives each role only the permissions it actually needs. If credentials are misused or leaked, the limited scope contains the blast radius. It is a core security control, distinct from performance or cost concerns."),
        ],
    ),
]

# ---------------- WEEKS 11-24: Capstone pipeline (progressive) ----------------
CAPSTONE = [
    ("Capstone kickoff: scope and architecture",
     "Choose a real public data source and design the end-to-end architecture before writing code.",
     [("Pick a source and define the question",
       "A capstone starts with one business question and a public source that can answer it.",
       "Scoping to one question keeps a 14-week build from sprawling into nothing shipped.",
       "Write the one-liner that anchors everything:\n\n```text\nQuestion: how do daily bike trips vary by weather?\nSource:   public bike-share + weather APIs\n```",
       "WRITE", "Write your capstone's single guiding question and the public source that answers it.",
       ["You stated one guiding question", "You named a concrete public source"]),
      ("Draw the target architecture",
       "The architecture names every layer from ingestion to dashboard and the tool at each.",
       "A diagram now prevents three rebuilds later.",
       "Lay out the layers you will build:\n\n```text\nAPI -> ingest -> lake (Parquet) -> warehouse -> dbt -> dashboard\n```",
       "PRODUCE", "Produce ARCHITECTURE.md with a diagram of all pipeline layers and chosen tools.",
       ["ARCHITECTURE.md shows every layer", "Each layer names a tool"]),
      ("Define the data contract",
       "Agree the schema, freshness, and ownership of each dataset up front.",
       "A contract makes downstream breakage a caught event, not a surprise.",
       "Pin the contract for your raw source:\n\n```text\nraw.trips: trip_id, start_ts, end_ts, station_id  | daily | owner: you\n```",
       "WRITE", "Document the schema and freshness contract for your raw source.",
       ["You listed the raw schema", "You set a freshness expectation"]),
      ("Set up the repo and environments",
       "A clean repo with separate dev and prod targets is the foundation for reproducible work.",
       "Branch-based dev and prod targets keep experiments from corrupting real data.",
       "Initialise the project skeleton:\n\n```bash\ngit init capstone && mkdir ingest transform dags\n```",
       "CODE", "Create the capstone repo with folders for ingestion, transformation, and orchestration.",
       ["The repo has the three folders", "It is initialised under git"]),
      ("Provision the warehouse and lake",
       "Create the storage bucket and warehouse dataset your pipeline will write to.",
       "Provisioning early surfaces access and credential issues before they block you.",
       "Create the landing locations:\n\n```bash\naws s3 mb s3://capstone-lake\n# + create warehouse dataset 'capstone'\n```",
       "CODE", "Provision an object-storage bucket and a warehouse dataset for the capstone.",
       ["A bucket exists", "A warehouse dataset exists"]),
      ("Plan the schedule and SLAs",
       "Decide how often each layer runs and what freshness users can expect.",
       "Explicit SLAs turn vague expectations into testable promises.",
       "Set the cadence per layer:\n\n```text\ningest: hourly | transform: hourly | dashboard SLA: <2h stale\n```",
       "WRITE", "Define run cadence for each layer and the end-to-end freshness SLA.",
       ["You set a cadence per layer", "You stated an end-to-end SLA"]),
      ("Capstone kickoff review",
       "Review the full plan against the guiding question before building.",
       "A plan review is the cheapest place to catch a wrong assumption.",
       "Checklist the plan is complete:\n\n```text\nquestion, source, architecture, contract, repo, schedule -> all set?\n```",
       "PRODUCE", "Commit a PLAN.md summarising scope, architecture, contract, and schedule.",
       ["PLAN.md covers all four areas", "It is committed to the repo"])]),
    ("Ingestion layer: extract from source",
     "Build the extraction code that pulls raw data from your source into a landing area.",
     [("Understand the source API",
       "Read the source's API docs for endpoints, pagination, rate limits, and auth.",
       "Most ingestion bugs trace back to misread pagination or rate limits.",
       "Probe the endpoint before coding:\n\n```bash\ncurl 'https://api.example-source.org/trips?date=2026-06-06' | head\n```",
       "WRITE", "Document the endpoint, auth method, pagination, and rate limit of your source.",
       ["You documented auth and pagination", "You noted the rate limit"]),
      ("Write the extractor",
       "An extractor fetches data and writes it untouched to a landing path with a timestamp.",
       "Landing data raw and unmodified preserves your ability to reprocess later.",
       "Fetch and land raw bytes:\n\n```python\nresp = requests.get(url, params={'date': d})\nopen(f'raw/trips_{d}.json','wb').write(resp.content)\n```",
       "CODE", "Write an extractor that pulls one day of data and saves the raw response.",
       ["The extractor saves a raw file", "The filename includes the date"]),
      ("Handle pagination and rate limits",
       "Loop through pages and back off on rate-limit responses to fetch complete data politely.",
       "Skipping pagination silently truncates your dataset; ignoring limits gets you blocked.",
       "Page until exhausted with backoff:\n\n```python\nwhile next_page:\n    r = get(next_page)\n    if r.status_code == 429: time.sleep(backoff); continue\n    next_page = r.json().get('next')\n```",
       "CODE", "Extend the extractor to page through all results and back off on HTTP 429.",
       ["The extractor fetches all pages", "It backs off on rate-limit responses"]),
      ("Make extraction idempotent",
       "Re-extracting a date should overwrite, not duplicate, that date's landing file.",
       "Idempotent extraction lets you safely rerun failed days during backfills.",
       "Overwrite the date partition deterministically:\n\n```python\npath = f'raw/dt={d}/data.json'  # same path each run for date d\n```",
       "WRITE", "Explain how partition-per-date paths make re-extraction idempotent.",
       ["You described deterministic per-date paths", "You linked it to safe reruns"]),
      ("Log and handle failures",
       "Wrap extraction with logging and clear failure signals so the orchestrator can retry.",
       "An extractor that fails silently is worse than one that crashes loudly.",
       "Surface failures explicitly:\n\n```python\ntry: extract(d)\nexcept Exception as e: log.error('extract failed %s', d); raise\n```",
       "CODE", "Add logging and a non-zero exit on failure to the extractor.",
       ["Failures are logged", "The process signals failure to the caller"]),
      ("Validate the landed data",
       "Before declaring success, check the landed file is non-empty and parseable.",
       "A quick post-land check catches empty or truncated extracts immediately.",
       "Assert the landing is sane:\n\n```python\nassert os.path.getsize(path) > 0\njson.load(open(path))  # parseable\n```",
       "CODE", "Add a post-extraction validation that the landed file exists and parses.",
       ["A non-empty check runs", "A parse check runs"]),
      ("Ship the ingestion layer",
       "A complete ingestion layer extracts, paginates, lands idempotently, logs, and validates.",
       "Finishing ingestion gives every later layer real data to work on.",
       "Run the layer end to end for one day:\n\n```bash\npython ingest/extract.py --date 2026-06-06\n```",
       "PRODUCE", "Run the full ingestion for one date and commit the ingestion code.",
       ["A landed, validated file is produced", "The ingestion code is committed"])]),
    ("Raw storage in the lake",
     "Convert landed data to Parquet and organise it in partitioned lake storage.",
     [("Choose the file format",
       "Convert raw JSON/CSV landings to Parquet for cheap, typed, columnar storage.",
       "Parquet is the standard lake format because it slashes scan cost downstream.",
       "Convert a landing to Parquet:\n\n```python\npd.read_json('raw/dt=2026-06-06/data.json').to_parquet('lake/trips/dt=2026-06-06/part.parquet')\n```",
       "CODE", "Convert one landed file to partitioned Parquet in your lake.",
       ["A Parquet file exists in the lake", "It sits under a dated partition path"]),
      ("Design the partition scheme",
       "Partition lake tables by the column queries filter on most, usually date.",
       "The right partition key is the single biggest lever on downstream query cost.",
       "Lay out partitions by date:\n\n```text\nlake/trips/dt=2026-06-05/  lake/trips/dt=2026-06-06/\n```",
       "WRITE", "Justify your partition column choice based on how the data will be queried.",
       ["You chose a partition column", "You justified it from query patterns"]),
      ("Define the lake schema",
       "Declare explicit column names and types so downstream readers are not guessing.",
       "An explicit schema stops type drift from corrupting later joins.",
       "Pin types on write:\n\n```python\ndf = df.astype({'trip_id':'int64','start_ts':'datetime64[ns]'})\n```",
       "CODE", "Enforce an explicit schema when writing the Parquet output.",
       ["Columns have explicit types", "The write uses that schema"]),
      ("Add an open table format",
       "Layer Delta or Iceberg over the Parquet files to gain ACID and time travel.",
       "A table format protects readers from half-written, partial files.",
       "Write as a Delta table instead of bare Parquet:\n\n```python\ndf.write.format('delta').mode('append').save('lake/trips')\n```",
       "WRITE", "Explain what your chosen table format adds over the bare Parquet you started with.",
       ["You named ACID or time travel", "You linked it to reader safety"]),
      ("Make lake writes idempotent",
       "Overwriting a partition on rerun keeps the lake free of duplicates.",
       "Partition overwrite is the lake equivalent of the safe-rerun pattern.",
       "Overwrite just the affected partition:\n\n```python\ndf.write.mode('overwrite').option('partitionOverwriteMode','dynamic').save('lake/trips')\n```",
       "CODE", "Configure partition overwrite so reprocessing a date replaces rather than duplicates it.",
       ["Reprocessing a date does not duplicate rows", "Only the affected partition changes"]),
      ("Compact small files",
       "Many tiny files slow reads; compaction merges them into right-sized files.",
       "The small-files problem quietly throttles every query until you compact.",
       "Reduce file count on write:\n\n```python\ndf.repartition(1).write.parquet(path)  # fewer, larger files\n```",
       "WRITE", "Explain the small-files problem and how compaction fixes it.",
       ["You described read overhead from many files", "You described compaction"]),
      ("Ship the lake layer",
       "A complete lake layer stores typed, partitioned, table-format data idempotently.",
       "A reliable lake is the durable source of truth every later layer rebuilds from.",
       "Run conversion for a date range:\n\n```bash\npython lake/load.py --from 2026-06-01 --to 2026-06-06\n```",
       "PRODUCE", "Load several dates into the lake and commit the lake-loading code.",
       ["Multiple partitions exist in the lake", "The loading code is committed"])]),
    ("Loading to the warehouse",
     "Move curated lake data into warehouse staging tables ready for transformation.",
     [("Stage raw into the warehouse",
       "Load lake Parquet into staging tables that mirror the source shape with minimal change.",
       "Staging untouched data first keeps extraction and transformation cleanly separated.",
       "Create an external or loaded staging table:\n\n```sql\nLOAD DATA INTO capstone.stg_trips FROM FILES(format='PARQUET', uris=['gs://capstone-lake/trips/*']);\n```",
       "CODE", "Load lake data into a warehouse staging table.",
       ["A staging table holds the loaded rows", "It mirrors the source columns"]),
      ("External tables vs loading",
       "External tables query files in place; native loads copy data in for speed.",
       "Choosing in-place versus loaded storage trades freshness against query speed and cost.",
       "Compare the two access modes:\n\n```text\nexternal: query files in the lake directly (no copy)\nnative:   COPY into warehouse storage (faster scans)\n```",
       "WRITE", "Explain one case for an external table and one for a native load.",
       ["You gave an external-table case", "You gave a native-load case"]),
      ("Incremental loading",
       "Load only new partitions each run instead of reloading the entire history.",
       "Incremental loads keep cost flat as history grows.",
       "Load just the new date:\n\n```sql\nDELETE FROM capstone.stg_trips WHERE dt='2026-06-06';\nLOAD ... uris=['gs://capstone-lake/trips/dt=2026-06-06/*'];\n```",
       "CODE", "Implement an incremental load that processes only the latest partition.",
       ["Only new data is loaded each run", "Reruns do not duplicate rows"]),
      ("Type and encoding handling",
       "Reconcile lake types with warehouse types and handle nulls and encodings explicitly.",
       "Silent type coercion is a frequent source of wrong aggregates.",
       "Cast explicitly during load:\n\n```sql\nSELECT CAST(start_ts AS TIMESTAMP) AS start_ts, SAFE_CAST(distance AS FLOAT64) distance FROM ...\n```",
       "WRITE", "Identify two type mismatches likely between your lake and warehouse and how to handle them.",
       ["You named two mismatches", "You gave a handling approach"]),
      ("Load auditing",
       "Record rows loaded and load time so you can prove what landed and when.",
       "An audit row is your evidence when someone asks why a number changed.",
       "Log each load to an audit table:\n\n```sql\nINSERT INTO capstone.load_audit VALUES('stg_trips','2026-06-06', @rowcount, current_timestamp());\n```",
       "CODE", "Write a load-audit record capturing table, partition, row count, and timestamp.",
       ["An audit row is written per load", "It captures row count and time"]),
      ("Verify staging integrity",
       "Compare staged row counts against the lake to confirm nothing was lost.",
       "A reconciliation check catches partial loads before transformation hides them.",
       "Reconcile counts across layers:\n\n```sql\nSELECT (SELECT count(*) FROM stg_trips WHERE dt='2026-06-06') = @lake_count AS ok;\n```",
       "CODE", "Add a reconciliation check comparing staged and lake row counts.",
       ["Counts are compared across layers", "A mismatch is surfaced"]),
      ("Ship the loading layer",
       "A complete loading layer incrementally stages audited, reconciled data.",
       "Reliable staging is the clean input dbt will transform next.",
       "Run the incremental load for a date:\n\n```bash\npython load/stage.py --date 2026-06-06\n```",
       "PRODUCE", "Run the incremental load and commit the loading code.",
       ["Staging is populated incrementally", "The loading code is committed"])]),
    ("dbt staging models",
     "Build the staging layer in dbt that cleans and standardises raw warehouse data.",
     [("Set up dbt on the warehouse",
       "Connect dbt to the warehouse and point sources at your staging tables.",
       "A working dbt connection is the gate to the entire transformation layer.",
       "Declare the source and test the connection:\n\n```bash\ndbt debug   # confirms warehouse connectivity\n```",
       "CODE", "Connect dbt to your warehouse and run dbt debug successfully.",
       ["dbt debug reports a good connection", "Sources point at staging tables"]),
      ("Write staging models",
       "Staging models rename columns, cast types, and standardise values one source table at a time.",
       "A clean staging layer means every downstream model starts from consistent inputs.",
       "Clean one source in a stg model:\n\n```sql\nSELECT trip_id, CAST(start_ts AS timestamp) AS started_at, lower(city) AS city\nFROM {{ source('capstone','stg_trips') }}\n```",
       "CODE", "Write a staging model that renames, casts, and standardises one source table.",
       ["A stg model builds successfully", "It standardises names and types"]),
      ("Naming conventions",
       "Consistent prefixes (stg_, int_, fct_, dim_) make a project navigable.",
       "Conventions let any teammate predict where a model lives and what it does.",
       "Adopt a layered naming scheme:\n\n```text\nstg_*  raw-cleaned | int_* intermediate | fct_/dim_* marts\n```",
       "WRITE", "Document the naming convention you will use across the project.",
       ["You defined prefixes for each layer", "You gave an example per layer"]),
      ("Add staging tests",
       "Assert uniqueness and non-null on keys in every staging model.",
       "Testing at staging stops bad data before it spreads into marts.",
       "Test the grain key:\n\n```yaml\ncolumns:\n  - name: trip_id\n    tests: [unique, not_null]\n```",
       "CODE", "Add unique and not_null tests to your staging models and run dbt test.",
       ["Key tests are declared", "dbt test passes on clean data"]),
      ("Document the models",
       "Add descriptions to models and columns so the catalog and docs are meaningful.",
       "Descriptions written now save every future reader a Slack message.",
       "Describe a model in YAML:\n\n```yaml\nmodels:\n  - name: stg_trips\n    description: One row per bike trip, cleaned and typed.\n```",
       "WRITE", "Write descriptions for your staging models and key columns.",
       ["Models have descriptions", "Key columns are described"]),
      ("Materialization choices",
       "Choose views for cheap staging and tables where reuse cost justifies it.",
       "Right-sizing materialization keeps builds fast and bills low.",
       "Default staging to views:\n\n```sql\n{{ config(materialized='view') }}\n```",
       "WRITE", "Explain why staging models are usually views rather than tables.",
       ["You linked views to cheap, fresh staging", "You noted when a table is justified"]),
      ("Ship the staging layer",
       "A complete staging layer is tested, documented, and consistently named.",
       "Solid staging is the dependable input for the dimensional marts next week.",
       "Build and test the whole staging layer:\n\n```bash\ndbt build --select staging\n```",
       "PRODUCE", "Build and test all staging models and commit them.",
       ["All staging models build and pass tests", "They are committed to the repo"])]),
    ("dbt marts and the dimensional model",
     "Build fact and dimension marts that answer the capstone's business question.",
     [("Design the dimensional model",
       "Decide the fact grain and the dimensions needed to answer your question.",
       "Getting the fact grain right is the most important modelling decision you make.",
       "State the grain explicitly:\n\n```text\nfct_trips grain: one row per completed trip\ndims: dim_station, dim_date, dim_weather\n```",
       "WRITE", "Define your fact-table grain and list the dimensions you need.",
       ["You stated the fact grain", "You listed the dimensions"]),
      ("Build dimension models",
       "Dimensions provide descriptive context with surrogate keys.",
       "Clean dimensions are what make the fact table sliceable by business attributes.",
       "Build a dimension with a surrogate key:\n\n```sql\nSELECT generate_uuid() AS station_key, station_id, station_name, city\nFROM {{ ref('stg_stations') }}\n```",
       "CODE", "Build at least two dimension models with surrogate keys.",
       ["Two dimension models build", "Each has a surrogate key"]),
      ("Build the fact model",
       "The fact joins staging events to dimension keys and keeps the measures.",
       "A correct fact table is the join point for every analytical question.",
       "Assemble the fact with dimension keys:\n\n```sql\nSELECT t.trip_id, d.date_key, s.station_key, t.duration_min\nFROM {{ ref('stg_trips') }} t JOIN {{ ref('dim_date') }} d ON ...\n```",
       "CODE", "Build the fact model joining to your dimensions and keeping the measures.",
       ["The fact model builds", "It references the dimension keys"]),
      ("Add mart-level tests",
       "Test fact-to-dimension relationships and measure ranges.",
       "Mart tests are the last gate before numbers reach a dashboard.",
       "Assert the fact joins cleanly:\n\n```yaml\ncolumns:\n  - name: station_key\n    tests: [{relationships: {to: ref('dim_station'), field: station_key}}]\n```",
       "CODE", "Add relationships and range tests to the fact model.",
       ["A relationships test guards the fact", "A measure range test exists"]),
      ("Build a metrics layer",
       "Define reusable metrics (trips per day, average duration) once, on top of the fact.",
       "A metrics layer stops every analyst from defining revenue five different ways.",
       "Define a metric model:\n\n```sql\nSELECT date_key, count(*) AS trips, avg(duration_min) AS avg_duration\nFROM {{ ref('fct_trips') }} GROUP BY date_key\n```",
       "CODE", "Build a metrics model aggregating your fact to the reporting grain.",
       ["A metrics model builds", "It aggregates the fact correctly"]),
      ("Verify against the question",
       "Run the metric query and sanity-check it answers the guiding question.",
       "Tying the output back to the question keeps the build honest.",
       "Eyeball the answer to your question:\n\n```sql\nSELECT date_key, trips FROM metrics_trips ORDER BY date_key DESC LIMIT 7;\n```",
       "WRITE", "Show a query result that begins to answer your guiding question.",
       ["You produced a result tied to the question", "The numbers look plausible"]),
      ("Ship the marts layer",
       "A complete marts layer is a tested star schema plus a metrics model.",
       "The marts are what the dashboard and stakeholders will actually consume.",
       "Build and test the marts:\n\n```bash\ndbt build --select marts\n```",
       "PRODUCE", "Build and test all marts and commit them.",
       ["Marts build and pass tests", "They are committed"])]),
    ("Orchestrating the pipeline",
     "Wire ingestion, loading, and dbt into one scheduled, monitored pipeline.",
     [("Map the full DAG",
       "List every task and its dependencies from extract through dbt build.",
       "Seeing the whole DAG reveals ordering bugs before you automate them.",
       "Sketch the end-to-end order:\n\n```text\nextract >> land_lake >> stage_warehouse >> dbt_build >> notify\n```",
       "WRITE", "Write the full task list and dependency order for the pipeline.",
       ["You listed every task", "You stated the dependency order"]),
      ("Author the orchestration code",
       "Encode the DAG in your orchestrator with each task calling the right script.",
       "Codifying the DAG turns a manual runbook into a one-click pipeline.",
       "Wire tasks in the orchestrator:\n\n```python\nextract >> land >> stage >> dbt_build >> notify\n```",
       "CODE", "Author the orchestration DAG that runs all pipeline stages in order.",
       ["The DAG defines all stages", "Dependencies are correct"]),
      ("Schedule and backfill",
       "Set the schedule and run a backfill across historical dates.",
       "A clean backfill proves the pipeline is truly idempotent.",
       "Backfill a date range:\n\n```bash\nairflow dags backfill capstone -s 2026-06-01 -e 2026-06-06\n```",
       "CODE", "Schedule the pipeline and run a multi-day backfill successfully.",
       ["A schedule is set", "A backfill completes without duplicates"]),
      ("Add retries and idempotency",
       "Configure retries and ensure each task is safe to rerun.",
       "Retries plus idempotency are what let the pipeline heal itself overnight.",
       "Harden each task:\n\n```python\ndefault_args = {'retries': 3, 'retry_delay': timedelta(minutes=5)}\n```",
       "WRITE", "Confirm in writing that each task is idempotent and explain why.",
       ["You assessed each task's idempotency", "You justified the retry settings"]),
      ("Parameterise the run",
       "Pass the execution date through every task so backfills and reruns target the right partition.",
       "Threading the run date through avoids the classic 'always processes today' bug.",
       "Use the orchestrator's date macro:\n\n```python\nextract = Op(... op_kwargs={'date': '{{ ds }}'})\n```",
       "CODE", "Parameterise all tasks on the execution date.",
       ["Tasks receive the execution date", "Backfilled runs target the right partition"]),
      ("Test the orchestration",
       "Trigger a run and confirm the full chain succeeds end to end.",
       "An end-to-end test run is the truest proof the pipeline works.",
       "Trigger and watch one run:\n\n```bash\nairflow dags trigger capstone\n```",
       "CODE", "Trigger one full pipeline run and confirm every task succeeds.",
       ["Every task in one run succeeds", "Output reaches the marts"]),
      ("Ship the orchestrated pipeline",
       "A complete pipeline runs scheduled, parameterised, retryable end to end.",
       "Orchestration is what makes the pipeline run without you.",
       "Confirm the scheduled run produces fresh marts:\n\n```text\nscheduled run -> marts updated -> notification sent\n```",
       "PRODUCE", "Commit the orchestration code and a screenshot of a successful run.",
       ["The orchestration code is committed", "A successful run is evidenced"])]),
    ("Adding a streaming increment",
     "Add a near-real-time path so the pipeline reflects fresh events between batch runs.",
     [("Identify the streaming use case",
       "Find the part of your question that genuinely benefits from sub-minute freshness.",
       "Streaming only earns its complexity where latency truly matters.",
       "Name the latency-sensitive slice:\n\n```text\nbatch: daily trip summary | stream: live active-trip count\n```",
       "WRITE", "Identify which metric in your project justifies a streaming path and why.",
       ["You named a streaming metric", "You justified the latency need"]),
      ("Set up the event source",
       "Create a topic or stream and emit events that mirror your domain.",
       "A working stream is the substrate for everything real-time.",
       "Create the stream:\n\n```bash\nkafka-topics --create --topic trip-events --partitions 3\n```",
       "CODE", "Create a topic/stream and produce sample domain events to it.",
       ["A stream exists", "Sample events are produced"]),
      ("Write the consumer",
       "A consumer reads events and updates a near-real-time aggregate.",
       "The consumer is where streaming turns into a usable number.",
       "Consume and update a running count:\n\n```python\nfor msg in consumer: counts[msg.key] += 1\n```",
       "CODE", "Write a consumer that maintains a live aggregate from the stream.",
       ["A consumer reads the stream", "It maintains a live aggregate"]),
      ("Apply a time window",
       "Use a tumbling or sliding window to bound the streaming aggregate.",
       "Windows turn an infinite stream into finite, reportable numbers.",
       "Aggregate within a window:\n\n```text\nactive trips in the last 5 minutes (sliding window)\n```",
       "WRITE", "Specify the window type and size for your streaming metric and justify it.",
       ["You chose a window type and size", "You justified the choice"]),
      ("Handle duplicates and late events",
       "Make the consumer idempotent and decide how late events are treated.",
       "Late and duplicate events are the norm, not the exception, in streams.",
       "Dedupe by event id:\n\n```python\nif msg.id in seen: continue\nseen.add(msg.id)\n```",
       "WRITE", "Explain how you handle duplicate and late-arriving events.",
       ["You described deduplication", "You described a late-event policy"]),
      ("Serve the streaming metric",
       "Expose the live aggregate to the dashboard or a fast store.",
       "A streaming metric is only useful once something can read it.",
       "Write the aggregate where the dashboard can read it:\n\n```python\nredis.set('active_trips', count)\n```",
       "CODE", "Serve the live metric to a fast store or endpoint the dashboard can query.",
       ["The live metric is queryable", "The dashboard can read it"]),
      ("Ship the streaming increment",
       "A complete streaming path produces a windowed, idempotent live metric.",
       "The streaming path proves you can deliver both batch and real-time from one project.",
       "Confirm the live metric updates within seconds:\n\n```text\nproduce event -> consumer updates -> dashboard reflects < 5s\n```",
       "PRODUCE", "Demonstrate the live metric updating in near real time and commit the streaming code.",
       ["The live metric updates within seconds", "The streaming code is committed"])]),
    ("Data quality gates",
     "Add comprehensive quality checks and wire them into the pipeline and CI.",
     [("Inventory the risks",
       "List the realistic ways your data could be wrong at each layer.",
       "You can only test for failures you have thought to name.",
       "Enumerate per-layer risks:\n\n```text\ningest: missing days | stage: type drift | marts: broken joins\n```",
       "WRITE", "List the top data-quality risk at each pipeline layer.",
       ["You named a risk per layer", "Risks are specific to your data"]),
      ("Add source-freshness checks",
       "Assert that each layer's data is recent enough for its SLA.",
       "Freshness checks catch the silent failure of a pipeline that loads stale data.",
       "Configure freshness on the source:\n\n```yaml\nfreshness: {error_after: {count: 26, period: hour}}\n```",
       "CODE", "Add freshness checks aligned to your SLAs and run them.",
       ["Freshness rules match your SLAs", "The check runs in the pipeline"]),
      ("Add schema and key tests",
       "Test grain uniqueness, non-null keys, and referential integrity across marts.",
       "Key and relationship tests are the backbone of trustworthy marts.",
       "Guard the fact's grain and joins:\n\n```yaml\ntests: [unique, not_null]\n# plus relationships to each dimension\n```",
       "CODE", "Add grain and relationship tests across your marts and run dbt test.",
       ["Grain tests exist on facts", "Relationship tests guard joins"]),
      ("Add value and range tests",
       "Assert measures fall in plausible ranges and categoricals match allowed values.",
       "Range tests catch the unit errors and bad parses rules-of-thumb miss.",
       "Bound a measure and a category:\n\n```yaml\n- accepted_values: {values: ['member','casual']}\n# duration_min between 0 and 1440\n```",
       "CODE", "Add accepted-values and range tests to key columns.",
       ["An accepted-values test exists", "A range test exists"]),
      ("Add anomaly checks",
       "Alert when row volumes or key distributions move more than expected.",
       "Anomaly checks catch the incidents that pass every static rule.",
       "Flag a volume swing:\n\n```sql\nSELECT abs(today-avg7)/avg7 > 0.5 AS anomaly FROM daily_counts\n```",
       "WRITE", "Define one volume and one distribution anomaly check for your data.",
       ["You defined a volume check", "You defined a distribution check"]),
      ("Wire tests into CI",
       "Run the full test suite on every pull request to block regressions.",
       "CI enforcement is what makes quality durable rather than occasional.",
       "Gate merges on the suite:\n\n```yaml\n- run: dbt build   # models + tests; PR fails on any failure\n```",
       "CODE", "Add a CI job running the full test suite on pull requests.",
       ["CI runs the full suite", "A failing test blocks the merge"]),
      ("Ship the quality layer",
       "A complete quality layer covers freshness, schema, values, and anomalies, enforced in CI.",
       "Comprehensive gates are what let you trust the pipeline unattended.",
       "Run the entire gated build:\n\n```bash\ndbt build && dbt source freshness\n```",
       "PRODUCE", "Run the full quality suite green and commit the test and CI code.",
       ["The full suite passes", "Test and CI code are committed"])]),
    ("Governance, catalog, and lineage",
     "Document, classify, and expose lineage for the whole pipeline.",
     [("Assign ownership",
       "Name an owner for every dataset so questions and incidents have an address.",
       "Unowned data is the data nobody fixes when it breaks.",
       "Record owners per dataset:\n\n```text\nfct_trips -> you | dim_weather -> you | raw.trips -> you\n```",
       "WRITE", "Assign an owner to every dataset in your pipeline.",
       ["Every dataset has an owner", "Ownership is recorded in the repo"]),
      ("Document datasets",
       "Write clear descriptions for every model and key column.",
       "Documentation is the difference between a reusable asset and a mystery table.",
       "Describe a mart in YAML:\n\n```yaml\nmodels:\n  - name: fct_trips\n    description: One row per completed bike trip with weather context.\n```",
       "WRITE", "Document all marts and their key columns.",
       ["All marts are described", "Key columns are described"]),
      ("Classify sensitive data",
       "Tag any PII or sensitive columns and note required handling.",
       "Classification is the first step to handling sensitive data lawfully.",
       "Tag sensitive fields:\n\n```text\ndim_rider.email -> PII (restricted, masked for analysts)\n```",
       "WRITE", "Identify and classify any sensitive columns in your pipeline.",
       ["Sensitive columns are tagged", "Handling rules are stated"]),
      ("Expose lineage",
       "Generate lineage from dbt so anyone can trace a number to its source.",
       "Lineage is the fastest answer to 'where did this number come from'.",
       "Generate and view lineage:\n\n```bash\ndbt docs generate && dbt docs serve\n```",
       "PRODUCE", "Generate the lineage graph and commit a screenshot.",
       ["Lineage is generated", "A screenshot is committed"]),
      ("Set access policies",
       "Apply least-privilege grants and mask sensitive columns for broad roles.",
       "Access policy is where governance becomes enforced, not just documented.",
       "Grant scoped access:\n\n```sql\nGRANT SELECT ON capstone.fct_trips TO ROLE analyst;\n```",
       "WRITE", "Define the access roles for your pipeline and what each can see.",
       ["You defined at least two roles", "Sensitive columns are restricted"]),
      ("Publish a catalog entry",
       "Add your datasets to a catalog (or a CATALOG.md) with all the above.",
       "A catalog turns your governance work into something teammates can find.",
       "Compose a catalog record:\n\n```text\nfct_trips | owner: you | desc: ... | class: joins to PII | lineage: link\n```",
       "PRODUCE", "Publish a catalog entry for your core datasets.",
       ["Core datasets are cataloged", "Entries include owner, description, classification"]),
      ("Ship the governance layer",
       "A complete governance layer has owners, docs, classification, lineage, and access.",
       "Governance is what makes the pipeline safe to hand to a team.",
       "Confirm the governance checklist is complete:\n\n```text\nowners, docs, classification, lineage, access -> all done?\n```",
       "PRODUCE", "Commit the full governance documentation for the pipeline.",
       ["All five governance areas are covered", "Documentation is committed"])]),
    ("Serving layer and dashboard",
     "Build the dashboard that answers the guiding question for stakeholders.",
     [("Choose the serving approach",
       "Decide whether the dashboard reads marts directly or a BI semantic layer.",
       "The serving choice shapes performance, governance, and who can self-serve.",
       "Pick the serving path:\n\n```text\nBI tool -> reads fct/metrics marts (governed, fast)\n```",
       "WRITE", "Choose your serving approach and justify it against your users' needs.",
       ["You chose a serving approach", "You justified it for your users"]),
      ("Connect the BI tool",
       "Point a BI tool at the warehouse and confirm it reads your marts.",
       "A live connection is the bridge from pipeline to people.",
       "Connect and preview the mart:\n\n```text\nBI tool -> warehouse -> capstone.metrics_trips\n```",
       "CODE", "Connect a BI tool to the warehouse and preview a mart.",
       ["The BI tool connects", "It previews a mart"]),
      ("Build the core chart",
       "Build the single chart that most directly answers the guiding question.",
       "One sharp chart beats ten vague ones for answering a question.",
       "Plot the headline metric:\n\n```text\nline chart: daily trips over time, by weather band\n```",
       "PRODUCE", "Build the headline chart that answers your guiding question.",
       ["A chart answers the question", "It reads from your marts"]),
      ("Add filters and drilldowns",
       "Let users slice by the dimensions you built.",
       "Interactivity is what turns a static report into a tool.",
       "Wire dimension filters:\n\n```text\nfilters: date range, station, weather band\n```",
       "PRODUCE", "Add interactive filters for your key dimensions.",
       ["At least two filters work", "They use your dimensions"]),
      ("Validate the numbers",
       "Cross-check dashboard figures against a direct warehouse query.",
       "A dashboard nobody has reconciled is a dashboard nobody should trust.",
       "Reconcile dashboard vs query:\n\n```sql\nSELECT sum(trips) FROM metrics_trips WHERE dt BETWEEN ...;\n```",
       "CODE", "Reconcile a dashboard figure against a direct SQL query.",
       ["A figure is reconciled to SQL", "They match"]),
      ("Polish and annotate",
       "Add titles, units, and a short note on data freshness and caveats.",
       "Annotations are what stop stakeholders from misreading the chart.",
       "Label everything clearly:\n\n```text\ntitle, axis units, 'data as of <ts>', known caveats\n```",
       "WRITE", "Add clear titles, units, and a freshness note to the dashboard.",
       ["Charts have titles and units", "A freshness note is present"]),
      ("Ship the dashboard",
       "A complete serving layer is a validated, interactive, annotated dashboard.",
       "The dashboard is where the whole pipeline finally delivers value.",
       "Share the published dashboard:\n\n```text\npublish -> share link -> stakeholders can view\n```",
       "PRODUCE", "Publish the dashboard and commit a link and screenshot.",
       ["The dashboard is published", "A link and screenshot are committed"])]),
    ("Monitoring, alerting, and cost",
     "Make the running pipeline observable and keep its cost under control.",
     [("Define what to monitor",
       "Decide the signals that tell you the pipeline is healthy: freshness, success, volume, cost.",
       "You cannot operate what you cannot see; pick signals before incidents force you to.",
       "List the health signals:\n\n```text\nrun success, end-to-end freshness, row volume, daily cost\n```",
       "WRITE", "List the four signals you will monitor and the healthy range of each.",
       ["You listed four signals", "Each has a healthy range"]),
      ("Pipeline run monitoring",
       "Track task success, duration, and SLA breaches in the orchestrator.",
       "Run monitoring turns silent failures into pages you can act on.",
       "Surface run health:\n\n```text\norchestrator UI -> success rate, durations, SLA misses\n```",
       "CODE", "Configure run monitoring and confirm a failed task surfaces.",
       ["Run health is visible", "A failure is surfaced"]),
      ("Set up alerting",
       "Route failures and SLA breaches to a channel you actually watch.",
       "An alert nobody sees is the same as no alert.",
       "Wire an alert on failure:\n\n```python\non_failure_callback = notify_slack\n```",
       "CODE", "Configure alerting on failure and SLA breach to a real channel.",
       ["Alerts fire on failure", "They reach a channel you watch"]),
      ("Data freshness dashboards",
       "Build a small ops view of when each dataset last updated.",
       "A freshness view is the first place to look when a number seems stale.",
       "Track last-loaded per dataset:\n\n```sql\nSELECT table_name, max(loaded_at) FROM load_audit GROUP BY 1;\n```",
       "CODE", "Build a freshness view from your load-audit data.",
       ["A freshness view exists", "It reads from load audit"]),
      ("Cost monitoring",
       "Track warehouse and storage spend and attribute it to pipeline stages.",
       "Untracked cost is how a pipeline quietly becomes a budget problem.",
       "Watch spend by stage:\n\n```text\nwarehouse bytes scanned + compute time + lake storage = daily cost\n```",
       "WRITE", "Identify your top two cost drivers and one action to reduce each.",
       ["You named two cost drivers", "Each has a reduction action"]),
      ("Cost optimisation",
       "Apply partition pruning, right-sized compute, and incremental builds to cut cost.",
       "Most pipelines can cut cost substantially without touching correctness.",
       "Apply a concrete saving:\n\n```sql\n-- ensure dashboards filter on the partition column\nWHERE dt BETWEEN @start AND @end\n```",
       "CODE", "Implement one cost optimisation and measure the before/after spend.",
       ["An optimisation is applied", "You measured the cost change"]),
      ("Ship observability",
       "A complete observability layer covers run health, freshness, alerting, and cost.",
       "Observability is what lets you sleep while the pipeline runs.",
       "Confirm the observability checklist:\n\n```text\nruns, freshness, alerts, cost -> all monitored?\n```",
       "PRODUCE", "Commit the monitoring, alerting, and cost-tracking configuration.",
       ["All four areas are monitored", "Configuration is committed"])]),
    ("CI/CD and infrastructure as code",
     "Automate testing and deployment of the pipeline and codify its infrastructure.",
     [("Version everything",
       "Ensure pipeline code, dbt project, and infra definitions all live in git.",
       "Anything not in version control is a single hard-drive failure from gone.",
       "Confirm the repo holds it all:\n\n```text\ningest/ transform/ dags/ infra/ -> all in git\n```",
       "WRITE", "Confirm every component of your pipeline is version-controlled.",
       ["All components are in git", "Nothing critical lives only locally"]),
      ("CI for transformations",
       "Run dbt build and tests on every pull request in CI.",
       "CI on transformations stops broken models from ever merging.",
       "Gate the PR on a clean build:\n\n```yaml\n- run: dbt build --target ci\n```",
       "CODE", "Add a CI workflow that builds and tests dbt on pull requests.",
       ["CI builds and tests dbt", "A failure blocks the PR"]),
      ("CD to production",
       "On merge to main, deploy the transformation build to the production target.",
       "Automated deploys remove the error-prone manual production step.",
       "Deploy on merge:\n\n```yaml\non: {push: {branches: [main]}}\n- run: dbt build --target prod\n```",
       "CODE", "Add a deploy workflow that runs on merge to main against production.",
       ["A deploy runs on merge to main", "It targets production"]),
      ("Infrastructure as code",
       "Define the bucket, warehouse dataset, and roles in Terraform so they are reproducible.",
       "IaC turns 'click here in the console' into a reviewable, repeatable file.",
       "Codify a resource:\n\n```hcl\nresource \"google_storage_bucket\" \"lake\" { name = \"capstone-lake\" }\n```",
       "CODE", "Define your core infrastructure in Terraform and apply it.",
       ["Infra is defined in Terraform", "terraform apply succeeds"]),
      ("Manage secrets and config",
       "Keep credentials in a secret store and config in environment variables, never in git.",
       "A committed credential is a breach waiting to happen.",
       "Reference secrets, never inline them:\n\n```bash\nexport WAREHOUSE_KEY=$(read from secret manager)\n```",
       "WRITE", "Document where each secret and config value lives and how it is injected.",
       ["Secrets live outside git", "Injection is documented"]),
      ("Promote across environments",
       "Use separate dev and prod targets so changes are tested before production.",
       "Environment promotion is what lets you experiment without risking real data.",
       "Separate the targets:\n\n```text\ndev target -> sandbox dataset | prod target -> production dataset\n```",
       "WRITE", "Describe your dev-to-prod promotion flow for a transformation change.",
       ["You described dev and prod targets", "You described the promotion step"]),
      ("Ship CI/CD and IaC",
       "A complete delivery layer has CI tests, automated deploys, and IaC.",
       "CI/CD plus IaC is what makes the pipeline a maintainable product, not a one-off.",
       "Confirm the delivery checklist:\n\n```text\nCI, CD, IaC, secrets, promotion -> all in place?\n```",
       "PRODUCE", "Commit the CI/CD workflows and Terraform for the pipeline.",
       ["CI/CD workflows are committed", "Terraform defines the infra"])]),
    ("Capstone ship: docs, demo, and portfolio",
     "Finish, document, and present the complete data pipeline as a portfolio piece.",
     [("Write the README",
       "A strong README explains the problem, architecture, how to run it, and the result.",
       "The README is what a hiring manager reads first and often only.",
       "Cover the essentials:\n\n```text\nproblem, architecture diagram, run steps, results, lessons\n```",
       "PRODUCE", "Write a complete README for the capstone repository.",
       ["README covers problem and architecture", "It includes run steps and results"]),
      ("Document the architecture",
       "Finalise the architecture diagram and a short design-decisions note.",
       "Explaining your trade-offs shows the judgement employers actually hire for.",
       "Record key decisions:\n\n```text\nwhy batch+stream, why this warehouse, why these tests\n```",
       "WRITE", "Write a design-decisions note explaining three key trade-offs you made.",
       ["You explained three decisions", "Each names the trade-off"]),
      ("Record a demo",
       "Record a short walkthrough from raw source to live dashboard.",
       "A two-minute demo proves the pipeline runs more convincingly than any text.",
       "Show the full flow on camera:\n\n```text\ntrigger run -> data flows -> dashboard updates\n```",
       "PRODUCE", "Record a short demo video of the pipeline end to end and link it.",
       ["The demo shows end-to-end flow", "It is linked in the README"]),
      ("Reproducibility check",
       "Confirm someone else could clone and run the pipeline from the README alone.",
       "A pipeline only you can run is a liability, not an asset.",
       "Dry-run the setup from scratch:\n\n```bash\ngit clone ... && follow README -> pipeline runs\n```",
       "CODE", "Follow your own README from a clean checkout and fix any gaps.",
       ["A clean checkout runs from the README", "Gaps you found are fixed"]),
      ("Publish the portfolio piece",
       "Push the repo public and write a short post explaining what you built.",
       "Shipping in public is what turns learning into a credential.",
       "Announce the work:\n\n```text\npublic repo + a short write-up of problem, stack, and result\n```",
       "PRODUCE", "Publish the repo and a short write-up of the project.",
       ["The repo is public", "A write-up explains the project"]),
      ("Reflect on the build",
       "Note what worked, what broke, and what you would do differently.",
       "Reflection is what converts one project into transferable skill.",
       "Capture the lessons honestly:\n\n```text\nwins, hardest bug, next-time changes\n```",
       "WRITE", "Write an honest retrospective of the capstone build.",
       ["You named what worked", "You named what you would change"]),
      ("Ship The Forge capstone",
       "The finished pipeline ingests, transforms, governs, serves, and is fully documented.",
       "This is the portfolio centrepiece that proves you can build real data systems.",
       "Tag the final release of your capstone:\n\n```bash\ngit tag capstone-v1.0 && git push --tags\n```",
       "PRODUCE", "Tag the capstone v1.0 release and confirm the README, demo, and dashboard are all linked.",
       ["The capstone is tagged v1.0", "README, demo, and dashboard are all linked"])]),
]

PHASES_CAP = "Capstone"


def build():
    weeks = []
    # Weeks 1-10
    for i, w in enumerate(WEEKS, start=1):
        days = []
        for d_idx, (t, c, why, prac, lbl, instr, checks) in enumerate(w["days"], start=1):
            days.append({
                "number": d_idx,
                "title": t,
                "summary": f"Focus: {t}",
                "items": [
                    {"kind": "lesson", "title": t, "body": lesson_body(t, c, why, prac)},
                    ex(lbl, instr, checks),
                ],
            })
        cc = [{"q": q, "choices": ch, "correct": corr, "explain": expl} for (q, ch, corr, expl) in w["cc"]]
        weeks.append({
            "number": i, "title": w["title"], "phase": w["phase"],
            "commitment_hours": "12-18", "context": w["context"],
            "concept_check": cc, "days": days,
        })

    # Weeks 11-24 (capstone)
    for j, (title, ctx, daylist) in enumerate(CAPSTONE, start=11):
        days = []
        for d_idx, (t, c, why, prac, lbl, instr, checks) in enumerate(daylist, start=1):
            days.append({
                "number": d_idx, "title": t, "summary": f"Focus: {t}",
                "items": [
                    {"kind": "lesson", "title": t, "body": lesson_body(t, c, why, prac)},
                    ex(lbl, instr, checks),
                ],
            })
        cc = [
            (f"In '{title.lower()}', what is the single most important thing to get right?",
             ["Using the newest tool", "Delivering the layer's goal reliably and idempotently",
              "Writing the most code", "Avoiding documentation"], 1,
             "Each capstone week ships one reliable layer of the pipeline. The goal is correct, idempotent, documented output that the next layer can depend on. Chasing novel tools or volume of code misses the point of building a trustworthy system."),
            (f"Why does this capstone week end with a 'ship' deliverable committed to the repo?",
             ["To pad the schedule", "Because shipping and versioning each layer makes the build reproducible and reviewable",
              "Because git is mandatory", "To use more storage"], 1,
             "Committing a working deliverable each week keeps the project reproducible and reviewable. It also means every layer is a stable foundation for the next. Shipping incrementally is how real data platforms are built and maintained."),
            (f"What makes the output of this week trustworthy for downstream layers?",
             ["It looks impressive", "It is tested, idempotent, and documented so others can rely on it",
              "It is large", "It avoids dependencies"], 1,
             "Downstream layers can only build safely on output that is tested, safe to rerun, and documented. Those properties are what let a teammate or your future self depend on it without re-verifying everything. Size or visual polish do not create trust."),
        ]
        weeks.append({
            "number": j, "title": title, "phase": PHASES_CAP,
            "commitment_hours": "12-18", "context": ctx,
            "concept_check": [{"q": q, "choices": ch, "correct": corr, "explain": expl} for (q, ch, corr, expl) in cc],
            "days": days,
        })

    out = {"slug": "data-engineering", "title": "Data Engineering", "total_weeks": len(weeks), "weeks": weeks}
    (HERE / "data-engineering-src.json").write_text(json.dumps(out, indent=2, ensure_ascii=False), encoding="utf-8")
    print(f"Wrote data-engineering-src.json with {len(weeks)} weeks.")


if __name__ == "__main__":
    build()
