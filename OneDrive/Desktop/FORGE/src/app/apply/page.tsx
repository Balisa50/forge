/**
 * /apply, generic FORGE application page (no mentor scoped).
 * For per-mentor links see /apply/[mentorId]/page.tsx
 */
import ApplyForm from "./ApplyForm";

export default function ApplyPage() {
 return <ApplyForm />;
}
